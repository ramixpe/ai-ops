# Archive — completed process documents

These did their job and are no longer live reference. They are **kept, not
deleted**, for one reason: each is the record of an *intention*, and this
project's method is that what was intended is what makes what happened
meaningful. Several are cited by `FINDINGS.md` entries that would otherwise
dangle.

Archived 2026-08-18 (M0), with operator sign-off. Nothing here is current — for
current state read `docs/build/BACKLOG.md`, `docs/build/PROCESS.md` and
`docs/build/FINDINGS.md`.

| File | What it was | Superseded by |
|---|---|---|
| `REVIEW.md` | The first external review, 2026-07-28, at 32 tests | five later reviews, incl. `HOLISTIC-REVIEW.md` |
| `PLAN-V2.md` | The plan Gate Zero reconciled the backlog against | `BACKLOG.md`'s reconciled states |
| `BACKLOG-COMPLETION-PLAN.md` | A completion plan for the backlog | executed |
| `FIX-PLAN.md` | The six-agent fix wave | executed |
| `OPS-WAVE-PLAN.md` | The OPS wave plan | executed — B-477…B-480 shipped |
| `BACKLOG-STATUS.md` | Point-in-time verification of 98 items | `BACKLOG.md`, now 129 rows |
| `VERIFICATION.md` | Point-in-time claim audit | superseded by the same |
| `PEER-REVIEW-BRIEF.md` | Brief handed to a reviewer | the reviews it produced |
| `REPO-INVENTORY.md` | Pre-publication tree inventory | the tree moved under it |

Two files were **deleted** rather than archived in the same pass, because they
had no evidential value: `docs/devices.md` (generated by `devices_doc.py` from
the inventory, and stale) and `docs/build/AUTONOMOUS-LOG.md` (a session log
whose content is in `FINDINGS.md` and the commit history). Git retains both.

## Second pass — 2026-08-20, ahead of v1.0.0

Five more files, following the same reasoning: each did its job, each is still
referenced by something else (so deleting rather than archiving would create
dangling references), and none is a live reference any more. No deletions this
time.

| File | What it was | Superseded by |
|---|---|---|
| `BUILD-PLAN.md` | The 34-task MVP-0 plan, Parts 1-8 (Part 0 was extracted first — see below) | executed; reviewed at M4 in `docs/build/MVP0-REVIEW.md` |
| `TRACKER.md` | Progress record for that same plan, authoritative on its per-task status | the plan is complete; current project status is `docs/build/BACKLOG.md` |
| `SESSION-HANDOVER.md` | A build-state snapshot from 2026-08-18 into 2026-08-19 (its own Q-020 is already answered in it — "clause 2 dropped") | `docs/build/BACKLOG.md` for current state |
| `OPERATOR-RUNBOOK.md` | A step-by-step checklist for a specific lab window, 2026-08-19 | partially — B-463 closed it, but B-440/round 6 is still `BLOCKED` in `BACKLOG.md`, so this is a historical checklist, not a completed one |
| `M0-DOC-RETIREMENT.md` | The kill-list the first archive pass (above) executed from | executed; kept as the record of that decision |

**Not archived, and why:** `BUILD-PLAN.md`'s **Part 0** — the rules of
engagement (model roles, the escalation ladder, the frozen-files list, the
FINDINGS.md entry format) — is not history. It is the process contract this
build has operated under since 2026-08-15 and dozens of comments across
`src/`, `tests/` and `scripts/` cite it by section number (`` `BUILD-PLAN.md`
§0.N ``). It was extracted verbatim, section numbers unchanged, to
[`docs/build/PROCESS.md`](../build/PROCESS.md), which stays live.
