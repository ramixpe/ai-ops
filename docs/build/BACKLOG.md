# Backlog

Work beyond MVP-0, staged. Nothing here is scheduled — this is the ordered list of what exists to be done, so that finishing MVP-0 does not end in an empty room.

**MVP-0 is not in this file.** It lives in `BUILD-PLAN.md` as tasks T-001 to T-035. Nothing below starts until the findings log from that build has been reviewed.

**Sizes** are rough: `S` a day or less · `M` a few days · `L` a week or more · `?` unknown until a discovery item resolves.

**Every item names the decision it comes from**, so its rationale is one lookup away in `docs/design/design-thinking.md`.

---

## The governing rule

> Over-engineering is building *N* of something before validating one.

Which is why the backlog is deliberately shallow in places it could be deep. There is one flow, not seven. One platform, not three. One prompt library, not a prompt framework. Each of those becomes N only after its one has been proven against the real fabric.

---

# MVP-1 — the adaptive layer

MVP-0 proves the deterministic descent. MVP-1 adds the parts where the model gets to influence what happens next, each fenced by a typed contract.

| ID | Item | Why | Depends on | Size | Decision |
## Reconciliation — 2026-08-17 (PLAN-V2 Gate Zero)

Every item carries **state**, **evidence**, **dependencies with their states**, and
**last touched**. Produced because the previous plan was written from item titles and
scheduled four completed items and a track with no runnable work (OBS-123, OBS-124).

`unverified` is **not** `OPEN`. It means nobody has checked — a different state from
checked-and-still-open, and conflating them is what the last plan did.

### The vocabulary was one state short — corrected 2026-08-17

The first pass had `BLOCKED` (*cannot be done*) and `unverified` (*nobody has looked*)
and **no state for "examined, deferred, and here is the condition that would change
that"**. So `B-443`–`B-450` — the peer review's §5 deferrals, each carrying an explicit
trigger, and the **best-documented items in this file** — were marked `unverified`,
which asserts the opposite of what is true about them. Three reviewers looked at them
in detail.

> **`DEFERRED` — examined, not scheduled, with the unblocking condition as its
> evidence.** `BLOCKED` is *cannot*; `DEFERRED` is *chose not to, and this is what
> would change it*.

This is Gate Zero's own lesson recurring one level down. Gate Zero exists because a
plan was written from item titles; the reconciliation then read those items through a
vocabulary that could not express what they said. **A state column is only as accurate
as the states available to it**, and a missing state does not fail loudly — it
silently rounds to the nearest one, which is how eight examined items came to be
labelled unexamined (OBS-136).

### Gate Zero completed — 2026-08-17

All 33 `unverified` items were read and given a real state. **Counts, 98 items** (B-465 and
B-466 filed 2026-08-17 from the MCP re-test)**:**

| | |
|---|---|
| `DONE` | **36** |
| `DEFERRED` | **25** — examined, unscheduled, each carrying its unblocking condition |
| `OPEN` | **20** — valid and startable when its phase begins |
| `BLOCKED` | **16** — cannot proceed |
| `CLOSED-AS-MEASURED` | **1** |
| `unverified` | **0** |

**96, not 95.** B-464 had been filed *into the reconciliation table in the main table's
format* — item, title, description — so it carried no state, no dependency and no
last-touched, and every count taken since has silently excluded it. A row that is
well-formed for the wrong table is invisible to a reader and to a `grep` for states.
Split into both tables and counted.

**What "examined" means here, stated precisely so the zero is not over-read.** For each
item this pass established that it is real, that its description still matches the
repository, that its **stated dependencies are accurate**, and that it belongs to a named
phase. It did **not** re-derive whether each item is the right thing to build, and it did
not design any of them. An item at `OPEN` is one nobody has argued against, not one
anybody has argued for.

**The zero was reached by reading, not by relabelling** — which was the thing to avoid,
since a backlog reaching zero `unverified` by reclassification would have reproduced, in
its own bookkeeping, the failure this reconciliation exists to correct.

**Three dependencies turned out to be stale, and each was silently holding an item back:**

- **B-110** (`l3vpn_service` flow) waited on a subject naming scheme. **Q-004 accepted
  `<pe>:<vrf>` (OBS-035)** — the blocker was resolved and the item never updated.
- **B-202** (mnemonic → flow lookup) waited on T-004 and T-015. **Both DONE.** Unblocked,
  merely unscheduled.
- **B-209** (report relay hardening) was `unverified` and is in fact **`BLOCKED`**: T-035
  is `TODO` and gated on Q-007's residency decision, so there is no relay to harden.

Two of those three had been startable for some time and read as unexamined. That is the
cost of the state a reconciliation cannot express, measured (OBS-140).

| Item | State | Evidence | Depends on (state) | Last touched |
|---|---|---|---|---|
| **B-101** | `OPEN` | PLAN-V2 P2.1 | MVP-0 complete | OBS-124 |
| **B-102** | `OPEN` | reasoning gate, half 2. D7; PLAN-V2 P2.1 | B-101 (OPEN) | OBS-140 |
| **B-103** | `OPEN` | narrowing pass. D6; PLAN-V2 P2.1 | B-101 (OPEN), B-102 (OPEN) | OBS-140 |
| **B-104** | `OPEN` | config axis. D16; **dependency satisfied — MVP-0 shipped at M4** | — (was: MVP-0 complete) | OBS-140 |
| **B-105** | `OPEN` | inheritance resolution. D16 | B-104 (OPEN) | OBS-140 |
| **B-106** | `OPEN` | intent-vs-observed diff. D16 | B-104 (OPEN) | OBS-140 |
| **B-107** | `OPEN` | PLAN-V2 P2.2 | MVP-0 complete | OBS-124 |
| **B-108** | `OPEN` | flow: `device_health`. PLAN-V2 P2.2 | B-107 (OPEN) | OBS-140 |
| **B-109** | `OPEN` | flow: `ldp_session`. PLAN-V2 P2.2 | B-107 (OPEN) | OBS-140 |
| **B-110** | `OPEN` | flow: `l3vpn_service`. **Blocker resolved — Q-004 accepted `<pe>:<vrf>` (OBS-035); the naming scheme it waited on exists** | — (was: T-006 finding) | OBS-140 |
| **B-111** | `OPEN` | PLAN-V2 P2.3 | T-006 | OBS-086 |
| **B-112** | `OPEN` | free-text flow selection. D5 | B-101 (OPEN) | OBS-140 |
| **B-113** | `OPEN` | rewording DONE 6637368; consolidation open, P1.4 | MVP-0 complete | OBS-117 |
| **B-114** | `OPEN` | gate model evaluation. D7 | B-101 (OPEN) | OBS-140 |
| **B-115** | `OPEN` | health-check pipeline. Part 7 open item 4; **MVP-0 shipped** | — (was: MVP-0 complete) | OBS-140 |
| **B-201** | `DEFERRED` | until Stage 2 — event-driven intake. B-426 is how it gets tested end to end | — | OBS-140 |
| **B-202** | `DEFERRED` | until Stage 2. **Dependencies satisfied — T-004 and T-015 both DONE**; unblocked, not scheduled | — (was: T-004, T-015) | OBS-140 |
| **B-203** | `DEFERRED` | until Stage 2 — operational memory. D14 | — | OBS-140 |
| **B-204** | `DEFERRED` | until B-203 | B-203 (DEFERRED) | OBS-140 |
| **B-205** | `DEFERRED` | until B-203 | B-203 (DEFERRED) | OBS-140 |
| **B-206** | `BLOCKED` | platform work B-206a/b | B-206 (BLOCKED), B-206 (BLOCKED) | OBS-124 |
| **B-207** | `DEFERRED` | until Stage 2, and **before it ships, not after** — the item says so | B-201 (DEFERRED) | OBS-140 |
| **B-208** | `DEFERRED` | until Stage 2 — this is the gate on the transition | B-201 (DEFERRED) | OBS-140 |
| **B-209** | `BLOCKED` | **Q-007 resolved 2026-08-17 — Telegram.** Now blocked only on T-035 itself: no relay exists to harden until one is built | T-035 (TODO) | OBS-140 · OBS-141 |
| **B-210** | `DEFERRED` | until Stage 2 memory. B-402 is DONE, so unblocked and unscheduled | B-402 (DONE) | OBS-140 |
| **B-301** | `DEFERRED` | until Stage 3. **Nothing else in Stage 3 starts without this** | — | OBS-140 |
| **B-302** | `DEFERRED` | until B-301 | B-301 (DEFERRED) | OBS-140 |
| **B-303** | `DEFERRED` | until B-302 | B-302 (DEFERRED) | OBS-140 |
| **B-304** | `DEFERRED` | until B-301 and B-303 | B-301, B-303 (DEFERRED) | OBS-140 |
| **B-305** | `DEFERRED` | until B-304 | B-304 (DEFERRED) | OBS-140 |
| **B-306** | `DEFERRED` | until B-305 | B-305 (DEFERRED) | OBS-140 |
| **B-307** | `DEFERRED` | until B-305 | B-305 (DEFERRED) | OBS-140 |
| **B-401** | `BLOCKED` | no Junos device | **a Junos device** | OBS-086 |
| **B-402** | `DONE` | OBS-103 | — | OBS-103 |
| **B-403** | `DONE` | OBS-123 | B-107 (OPEN) | OBS-123 |
| **B-404** | `DONE` | OBS-123 | MVP-0 | OBS-123 |
| **B-405** | `OPEN` | prompt library expansion. D18; MVP-1 | MVP-1 | OBS-140 |
| **B-406** | `DEFERRED` | until Stage 2 — gNMI telemetry. D8 | Stage 2 | OBS-140 |
| **B-407** | `OPEN` | session memory, multi-turn. D14; MVP-1, and small | MVP-1 | OBS-140 |
| **B-408** | `DEFERRED` | until Stage 2 — probe budgeting matters under event storms. D13 | Stage 2 | OBS-140 |
| **B-409** | `OPEN` | scale test. Part 6 — the device-count-independence claim is unmeasured | MVP-1 | OBS-140 |
| **B-410** | `DEFERRED` | until Stage 2 — a runbook for an unattended agent. D3 | Stage 2 | OBS-140 |
| **B-411** | `DONE` | OBS-123 | — | OBS-123 |
| **B-412** | `BLOCKED` | **outside this repository** — `~/ai-agent-ops/faultlab/`, which is not a git repo (OBS-135). Cannot be closed from here | — | OBS-075 · OBS-135 |
| **B-413** | `DONE` | OBS-059 | — | OBS-059 |
| **B-414** | `CLOSED-AS-MEASURED` | OBS-127 — 28 kept records; aggregation carries nothing | B-206 (BLOCKED) | OBS-124 |
| **B-415** | `BLOCKED` | OBS-124 | B-206 (BLOCKED) | OBS-124 |
| **B-416** | `BLOCKED` | OBS-124 | B-414 (BLOCKED) | OBS-124 |
| **B-417** | `BLOCKED` | OBS-124 | B-107 (OPEN) | OBS-124 |
| **B-418** | `BLOCKED` | OBS-124 | B-414 (BLOCKED) | OBS-124 |
| **B-419** | `BLOCKED` | OBS-124 | B-101 (OPEN), B-414 (BLOCKED) | OBS-124 |
| **B-420** | `DONE` | T-029a; verified OBS-124 | — | OBS-124 |
| **B-421** | `DONE` | OBS-102 | T-031 | OBS-102 |
| **B-422** | `DONE` | `_note` writes to stderr, stdout carries the payload. **Independently re-verified 2026-08-17**: a `test_notifier.py` assertion against stdout failed because the note was on stderr | — | VERIFICATION §6 |
| **B-423** | `DONE` | `.github/workflows/ci.yml` `offline-demo`: installs without the `llm` extra, asserts `.env` absent and 7 credential vars unset, checks all three exit codes | — | VERIFICATION §6 |
| **B-424** | `DONE` | OBS-081 | — | OBS-081 |
| **B-425** | `DONE` | OBS-123 | — | OBS-123 |
| **B-426** | `BLOCKED` | earned by rounds, not assumed | B-201 (unverified) | OBS-124 |
| **B-427** | `BLOCKED` | needs B-426 | B-426 (BLOCKED) | OBS-098 **CORPUS UPDATE 2026-08-17: round 7 is the fifth scored round** (1 IGP shutdown, 2 transport, 3 BGP admin-shut, 5 propagation, 7 LFA persistence). Round 7 is the first whose **full per-sample payloads are archived** (`evidence-archive/round7/`, 325 samples across two runs) rather than a finding — §6.1d in force from here. It is also the first scored round that validates *shipped code* rather than measuring diagnostic accuracy, so it belongs in the corpus under a different heading: **mechanism validation**, not a blind trial. Its prediction was sealed and pushed before the fault (`e02c6ca`) and §6.1b caught a harness/prediction fault mismatch before the seal rather than after. |
| **B-428** | `DONE` | OBS-124 | — | OBS-124 |
| **B-429** | `DONE` | OBS-083 | — | OBS-083 |
| **B-430** | `DONE` | OBS-097 | — | OBS-097 |
| **B-431** | `DONE` | OBS-097 | — | OBS-097 |
| **B-432** | `DONE` | OBS-124 | — | OBS-124 |
| **B-433** | `DONE` | OBS-100 | — | OBS-100 |
| **B-434** | `DONE` | OBS-100 | — | OBS-100 |
| **B-435** | `DONE` | `topology.hostname_map` + `resolve_device`. Both false anomaly classes go to **zero** on t0; the classes survive with synthetic coverage | — | OBS-103 · OBS-139 |
| **B-436** | `DONE` | 548e380 | — | OBS-108 |
| **B-437** | `DONE` | OBS-121 | — | OBS-121 |
| **B-438** | `DONE` | OBS-106 | — | OBS-106 |
| **B-439** | `DONE` | 79c6777 | — | OBS-122 |
| **B-440** | `BLOCKED` | needs the lab. **Prediction sealed 2026-08-17 — `ROUND-6.md`.** Predicts B-456 already prevents B's scenario for any fault leaving rung 3 healthy, and seals the residual (the `ALL_HEALTHY` fallback) separately | B-431 (DONE) | OBS-116 · OBS-141 |
| **B-441** | `DONE` | `MVP0-REVIEW.md:203` reports 3/3 on-path, 0/1 no-fault, 3/4 overall, and names the sampling frame | — | review §3.5 |
| **B-442** | `DONE` | OBS-116 | B-452 (BLOCKED) | OBS-116 |
| **B-443** | `DEFERRED` | until a second address family or a VRF-scoped session exists | — | review §5 · A+B |
| **B-444** | `DEFERRED` | until any multi-device concurrent deployment | — | review §5 · A+B |
| **B-445** | `DEFERRED` | until escalation-grade output; B-430 (DONE) already surfaces the far end's stated reason | — | review §5 · B |
| **B-446** | `DEFERRED` | until workflow adoption | — | review §5 · B |
| **B-447** | `DEFERRED` | until either the EVPN or the SR flow is designed | — | review §5 · A |
| **B-448** | `BLOCKED` | hardware — parked with B-401. *The payoff test is unvalidated, not passed* | B-401 (BLOCKED) | review §5 · A |
| **B-449** | `DEFERRED` | until any L3VPN or EVPN claim is made | — | review §5 · A |
| **B-450** | `DEFERRED` | until a characterised failure envelope exists | — | review §5 · C |
| **B-451** | `DONE` | f6ea6ec | B-436 (DONE) | OBS-109 |
| **B-452** | `BLOCKED` | deferred by decision, OBS-105 | — | OBS-116 |
| **B-453** | `DONE` | `check_identifier_containment`, wired into `ground_report`. 12 tests, **including two through `investigate()`** — the MCP re-test was expected to exercise this and did not (§10.3), so the wiring is now pinned rather than assumed | — | OBS-122 · OBS-138 |
| **B-454** | `DONE` | 6598df8 | B-436 (DONE) | OBS-122 |
| **B-455** | `DONE` | OBS-119 | B-436 (DONE) | OBS-119 |
| **B-456** | `DONE` | 503b2ac | B-437 (DONE), B-431 (DONE) | OBS-121 |
| **B-457** | `DONE` | OBS-126 | B-439 (DONE) | — |
| **B-458** | `DONE` | OBS-126 | — | OBS-113 |
| **B-459** | `DONE` | ee733a2 | B-453 (OPEN) | OBS-122 |
| **B-460** | `DONE` | OBS-120 | — | OBS-120 |
| **B-461** | `DONE` | numbered rungs, 61af0d0 | B-439 (DONE) | OBS-117 |
| **B-462** | `DONE` | OBS-129 — round 7, 99 down-port samples, 0 persisting | B-456 (DONE) | OBS-121 |
| **B-463** | `BLOCKED` | **`round8b.py` written and verified 2026-08-17; needs the lab.** Both round-8 defects fixed and tested against its real strings; §6.3 amended with the arithmetic. **round 8 ran and was scored; the primary claim is void, not answered.** §2a.3/§2a.4/§2a.5/§2a.6 CONFIRMED — rung 2 reads the socket (`samples_socket_not_reported: 0` on 363 samples), teardown in 3.11 s, BFD ruled out, and `peer in wrong AS` captured. §2a.2 unresolvable: the sampler's socket regex read `False` in all 195 Established samples, so the falsifier fired on an instrument that could not fail it (OBS-133). `OpenSent` was observed once, which proves the **fabric** produced the separating condition; whether rung 2 *reports* it is unmeasured. Needs **round 8b** — same fault, sub-200 ms sampling, raw line archived (`ROUND-8.md` §6) | B-437 (DONE) | OBS-121 · OBS-133 · OBS-134 |
| **B-464** | `BLOCKED` | needs the lab; **lower priority than any round that closes a boundary**. `fault_lab.py` option 6 unchanged; the sealed MD5 prediction stands as a recorded unrun claim in `ROUND-8.md` §3 | — | ROUND-8 §1.1 |
| **B-465** | `OPEN` | **half done.** The drift rule is direction-aware (an increase is `info`, names the stale baseline, and no longer raises a repaired device to `warning`). **Remaining: re-run `learn-topology`, which needs the lab** | — | OBS-142 · OBS-146 |
| **B-466** | `OPEN` | measured 23.1 s of a 30 s coherence bound on a healthy fabric (77%). The epoch may refuse on the tool's own latency | B-436 (DONE) | OBS-142 |
| **B-467** | `DONE` (agent-loop leg in wave 2-B) | `FREE_TEXT_FIELDS` typed table; `quote_device_text` strips embedded delimiters (the escape test); per-projection free-text budget withholds rather than drops. **The agent corrected my spec's field names** (`last_reset_reason`, `interface`) — the literal spec would have protected nothing for those entries | — | FIX-PLAN · wave 1-A |
| **B-468** | `DONE` | `_probe_exit` + `_envelope_exit` in `cli.py`; 6 tests both directions. Partial loss exits 0 deliberately and says why | — | DEEP-REVIEW §2.2-2.3 |
| **B-469** | `DONE` | catch narrowed to `ValueError`; `origin_unresolved` carried in the payload; the wrong comment (`unevaluated`) corrected against the code | — | DEEP-REVIEW §2.5 |
| **B-470** | `DONE` | `model_egress.py`: projector + canary matrix over four paths. evidence_budget's raw fallback removed; correlate bumped to v4 with the untrusted-content constraint | — | FIX-PLAN · wave 1-A |
| **B-471** | `OPEN` | **agent trust labelling** (P0-03 cheap half): `trust_class: exploratory`, unknown stop reason = incomplete not success, CLI label. Wave 2-B | B-470 (OPEN) | FIX-PLAN |
| **B-472** | `OPEN` | **agent deadline is real** (P1-01): absolute deadline propagated to provider timeouts; tools not started when budget exhausted. Wave 2-B | — | FIX-PLAN |
| **B-473** | `DONE` | `_active_probe_tool`: same sanitisation boundary via shared `_register_sanitized_tool`, distinct annotations (`open_world_hint` + ACTIVE PROBE title), docstring prefix. Signalling, not enforcement — stated | — | FIX-PLAN · wave 1-D |
| **B-474** | `DONE` | `_atomic_write_text` (tmp+fsync+replace), one-transaction golden + partial unique index with MAX(id) dedup migration, guarded reads with loud warnings, `snapshots_skipped` in `detect_flaps`, device-name validation both backends. **35 tests** | — | FIX-PLAN · wave 1-C |
| **B-475** | `OPEN` | **half done** (wave 1-D): `assess_lab_fabric_health` pooled, concurrency test proven non-vacuous by reversion. Remaining: the agent loop's `_assess_health` (wave 2-B); the full cross-surface scheduler stays future | — | FIX-PLAN |
| **B-476** | `DONE` | `settings.py`, 37 vars, two-direction cross-check test, `nettools config show/check`, startup warnings. Found `NETTOOLS_FIXTURE_DIR` undocumented and two live typo-swallows (`fasle` enables probes; `sqlit` selects files). Rewiring call sites is named future work | — | FIX-PLAN · wave 1-E |

**Totals:** `BLOCKED` 13 · `DONE` 33 · `OPEN` 7 · `unverified` 42 · **95 items**

*Updated 2026-08-17 after P1.1: B-457 and B-458 closed (OBS-126).*

---

|---|---|---|---|---|---|
| **B-101** | **The reasoning gate** — typed decision object, two shapes only | The model asks for more evidence without being able to invent a target | MVP-0 complete | M | D7 |
| **B-102** | **Candidate enumeration** — code derives narrowing targets from observed objects | The half of the gate that stops "the model chooses" becoming "the model invents" | B-101 | S | D7 |
| **B-103** | **Narrowing pass** — collect detail for the named object, re-enter reasoning | Completes the funnel; bounded at one pass until evidence says otherwise | B-101, B-102 | M | D6 |
| **B-104** | **Config axis** — `config_section.py`, section templates, projection | Observed-vs-intended is what turns a finding into "this is wrong" | MVP-0 complete | M | D16 |
| **B-105** | **Inheritance resolution** — expand `neighbor-group` / `session-group` / `af-group` | Unresolved inheritance forces the model to guess an expansion from its majority-dialect prior | B-104 | M | D16 |
| **B-106** | **Intent-vs-observed diff** — reconcile the two axes in code, hand the model the diff | The real answer to the context problem: the model reads the reconciliation, not the documents | B-104 | M | D16 |
| **B-107** | **Flow: `isis_adjacency`** | The most-shared dependency — every protocol above it descends through it **Its broken state is designed alongside the flow, not captured afterwards** — a healthy-only corpus has structurally zero coverage of the broken case, not weak coverage. The `broken` label caught three separate classes of defect the healthy fixtures could not reach: four parser gaps, the walk-semantics bug (Q-017), and the subject-vocabulary gap. See OBS-050, OBS-055, OBS-057. **Sequenced, then parallelisable — amended 2026-08-16.** The five do not start until **B-428 lands**. Then `isis_adjacency` runs **serially and alone**, to prove the pattern repeats on a second flow; only after it does do the remaining four run concurrently, one agent per flow, Opus 5 judging acceptance for all of them. Running five in parallel off a pattern demonstrated once would be five instances of the same unvalidated assumption, which is §0.13's data face at project scale. Contrast Part 4's T-029–T-032, which are a contract chain: running those concurrently means two agents guessing at a shape neither has settled, the same silent-disagreement failure as the T-024 subject vocabularies (OBS-057). | MVP-0 complete | S | D5 |
| **B-108** | **Flow: `device_health`** | Wraps the existing `health.py` verdicts as a flow, so "is PE1 ok" has an entry point **Its broken state is designed alongside the flow, not captured afterwards** — a healthy-only corpus has structurally zero coverage of the broken case, not weak coverage. The `broken` label caught three separate classes of defect the healthy fixtures could not reach: four parser gaps, the walk-semantics bug (Q-017), and the subject-vocabulary gap. See OBS-050, OBS-055, OBS-057. **Sequenced, then parallelisable — amended 2026-08-16.** The five do not start until **B-428 lands**. Then `isis_adjacency` runs **serially and alone**, to prove the pattern repeats on a second flow; only after it does do the remaining four run concurrently, one agent per flow, Opus 5 judging acceptance for all of them. Running five in parallel off a pattern demonstrated once would be five instances of the same unvalidated assumption, which is §0.13's data face at project scale. Contrast Part 4's T-029–T-032, which are a contract chain: running those concurrently means two agents guessing at a shape neither has settled, the same silent-disagreement failure as the T-024 subject vocabularies (OBS-057). | B-107 | S | D5 |
| **B-109** | **Flow: `ldp_session`** | Completes the MPLS control plane **Its broken state is designed alongside the flow, not captured afterwards** — a healthy-only corpus has structurally zero coverage of the broken case, not weak coverage. The `broken` label caught three separate classes of defect the healthy fixtures could not reach: four parser gaps, the walk-semantics bug (Q-017), and the subject-vocabulary gap. See OBS-050, OBS-055, OBS-057. **Sequenced, then parallelisable — amended 2026-08-16.** The five do not start until **B-428 lands**. Then `isis_adjacency` runs **serially and alone**, to prove the pattern repeats on a second flow; only after it does do the remaining four run concurrently, one agent per flow, Opus 5 judging acceptance for all of them. Running five in parallel off a pattern demonstrated once would be five instances of the same unvalidated assumption, which is §0.13's data face at project scale. Contrast Part 4's T-029–T-032, which are a contract chain: running those concurrently means two agents guessing at a shape neither has settled, the same silent-disagreement failure as the T-024 subject vocabularies (OBS-057). | B-107 | M | D5 |
| **B-110** | **Flow: `l3vpn_service`** | The one object type whose identity is not obvious — needs the naming scheme first **Its broken state is designed alongside the flow, not captured afterwards** — a healthy-only corpus has structurally zero coverage of the broken case, not weak coverage. The `broken` label caught three separate classes of defect the healthy fixtures could not reach: four parser gaps, the walk-semantics bug (Q-017), and the subject-vocabulary gap. See OBS-050, OBS-055, OBS-057. **Sequenced, then parallelisable — amended 2026-08-16.** The five do not start until **B-428 lands**. Then `isis_adjacency` runs **serially and alone**, to prove the pattern repeats on a second flow; only after it does do the remaining four run concurrently, one agent per flow, Opus 5 judging acceptance for all of them. Running five in parallel off a pattern demonstrated once would be five instances of the same unvalidated assumption, which is §0.13's data face at project scale. Contrast Part 4's T-029–T-032, which are a contract chain: running those concurrently means two agents guessing at a shape neither has settled, the same silent-disagreement failure as the T-024 subject vocabularies (OBS-057). | T-006 finding | M | D5 |
| **B-111** | **Flow: `topology`** | Multi-device; the fabric's LLDP data is self-contradictory, so this one must report disagreement rather than assert links **Its broken state is designed alongside the flow, not captured afterwards** — a healthy-only corpus has structurally zero coverage of the broken case, not weak coverage. The `broken` label caught three separate classes of defect the healthy fixtures could not reach: four parser gaps, the walk-semantics bug (Q-017), and the subject-vocabulary gap. See OBS-050, OBS-055, OBS-057. **Sequenced, then parallelisable — amended 2026-08-16.** The five do not start until **B-428 lands**. Then `isis_adjacency` runs **serially and alone**, to prove the pattern repeats on a second flow; only after it does do the remaining four run concurrently, one agent per flow, Opus 5 judging acceptance for all of them. Running five in parallel off a pattern demonstrated once would be five instances of the same unvalidated assumption, which is §0.13's data face at project scale. Contrast Part 4's T-029–T-032, which are a contract chain: running those concurrently means two agents guessing at a shape neither has settled, the same silent-disagreement failure as the T-024 subject vocabularies (OBS-057). | T-006 | M | D5 |
| **B-112** | **Free-text flow selection** | Lets a human ask "why is BGP down on PE2" instead of naming the flow | B-101 | S | D5 |
| **B-113** | **MCP tool consolidation** — ~22 tools to five stage-shaped ones | The manifest currently grows with the catalogue; this is D10/D11's failure mode live in the repo. **Reframed 2026-08-17 by measurement (OBS-112): less urgent than assumed, and the urgency moved.** `gemma-4-e4b` selected `investigate_lab_session` correctly from a 21-tool surface, and its reasoning trace named the *description* as the reason. Twenty tools say "collect read-only X"; one says what it achieves and when to prefer it. **So the count was not the binding constraint — the wording was.** Consolidation still has value (a smaller manifest is cheaper and easier to reason about), but the work that actually changes selection is writing every surviving description in terms of *what question it answers and when to prefer it*, and that work is worth doing **whether or not** consolidation happens. Do the wording first: it is cheaper, it is measurable the same way, and consolidating badly-worded tools into fewer badly-worded tools would have been credited with a fix it did not make | MVP-0 complete | M | D10, D11, D12 · OBS-112  **REWORDING DONE 2026-08-17** — all 21 descriptions now open `Answers: *<question>*` and say when to prefer the tool, pinned by a registry-driven test so a new tool inherits the form. Baseline captured verbatim in `MCP-EXPERIMENT.md` Appendix A; prediction pre-registered in §9 and pushed before the first edit. **CONSOLIDATION IS STILL OPEN, and its justification changed.** It was filed on D10/D11's manifest-growth argument and then reframed as less urgent than the wording. The rewording produced a better ground than either: **descriptions grew 5,961 → 11,107 chars, +86%**, and that is sent on every tool-list call, so the manifest a model reads before doing anything roughly doubled. **Five well-worded tools cost less context than 21 well-worded ones — and that argument needs no measurement at all.** It is arithmetic, it is available now, and it does not wait on the controlled comparison. Lead with it. The selection measurement remains worth running, but it is no longer what the case for consolidation rests on. |
| **B-114** | **Gate model evaluation** — can a local model emit the typed decision reliably? | Decides whether the reasoning path can be fully local. Grammar-constrained decoding is the fallback | B-101 | M | D7 |
| **B-115** | **Comprehensive health-check pipeline** | Breadth-first collect, no diagnostic question, summarise. The safest possible use of a model — pure summarisation of validated evidence | MVP-0 complete | M | Part 7 open item 4 |

**Sequencing note.** B-104 to B-106 (the config axis) and B-101 to B-103 (the gate) are independent. If the MVP-0 review shows the descent frequently ending in `undetermined`, do the config axis first — the missing evidence is more likely intent than depth.

---

# Stage 2 — event-driven

The agent stops being asked and starts being woken. The architectural change is small; the assurance change is not, because the boundaries now hold with nobody present.

| ID | Item | Why | Depends on | Size | Decision |
|---|---|---|---|---|---|
| **B-201** | **Trigger intake** — Alertmanager webhook receiver | Alertmanager already does grouping, dedupe, inhibition and silencing better than we would build it. **B-426 is how this gets tested end to end.** `inject → device syslog → Alertmanager → agent wakes → investigates → reports` is the entire Stage 2 loop, and fault injection is the only way to exercise it as one thing rather than as five components that each pass in isolation. A webhook receiver verified against a hand-crafted POST verifies the receiver; it says nothing about whether a real fault on a real device produces an alert that produces an investigation | T-005 finding | M | D4 · B-426 |
| **B-202** | **Mnemonic → flow lookup** | Flow selection for event-driven runs is a table lookup, not a model judgement. Syslog mnemonics are stable, enumerable identifiers | T-004, T-015 | S | D5 |
| **B-203** | **Operational memory: schema and writer** | `{timestamp, device, object, event_type, from_state, to_state, evidence_ref}`, written by code from validated envelopes  **Historical baselines and rarity are the same capability, arriving from the evidence-reduction side** (`evidence-reduction.md` §10): raw frequency is meaningless without a baseline — three BGP adjacency changes is severe if the historical expectation is zero, and thirty-one CPU threshold events is routine if the device normally produces twenty-eight to thirty-five. Both need schema'd events keyed by object, written by code from validated envelopes, queryable for "how often does this normally happen". **Build it once.** | MVP-1 complete | M | D14 |
| **B-204** | **Operational memory: query surface** | "Has this happened before, how often, when last" — the fourth evidence axis  **Historical baselines and rarity are the same capability, arriving from the evidence-reduction side** (`evidence-reduction.md` §10): raw frequency is meaningless without a baseline — three BGP adjacency changes is severe if the historical expectation is zero, and thirty-one CPU threshold events is routine if the device normally produces twenty-eight to thirty-five. Both need schema'd events keyed by object, written by code from validated envelopes, queryable for "how often does this normally happen". **Build it once.** | B-203 | M | D14 |
| **B-205** | **SQLite as the memory backend** | Files cannot answer "every interface that flapped five times this week". The flag already exists; this makes it the default for memory | B-203 | S | D14 |
| **B-206** | **Loki-backed `get_logs`** | **Not an upgrade to the local buffer** — corrected at T-028. Every event explaining the `broken` label is severity 5 or 6, which per OBS-041/B-206a never reaches Loki at all, so on the one incident this build captured the device buffer is *strictly better* and the Loki path would have returned nothing. B-206 earns its place on two things a device buffer structurally cannot do, neither about fidelity: **cross-device correlation** (one ordered timeline across three buffers, which is what makes B-415's clock skew real) and **reading a device that is unreachable** — the case where the historical axis matters most is the one where `show logging` cannot be run | T-004 finding · **blocked by B-206a and B-206b** | M | D8 · evidence-reduction.md §9 |
| **B-206a** | **Severity >=5 is dropped somewhere between the devices and Loki** | Only `err`(3) and `warning`(4) reach the collector, so `%BGP-5-ADJCHANGE` and IS-IS adjacency transitions -- the state changes correlation exists to explain -- never arrive. **Corrected 2026-08-15 (OBS-041): it is NOT the devices' trap level.** `show logging` on every device reports `Trap logging: level informational`, and the buffers hold severity 6 and 7 messages Loki never sees (555 buffered vs 50 shipped). The drop is further down the pipe -- syslog-ng handling or destination filtering. **A platform fix, not a Loki limitation** | T-004 finding (OBS-014), corrected by OBS-041 · Q-011 | S | D8 |
| **B-206b** | **The collector's own SSH churn is 97% of the corpus** | 1,187 of 1,219 lines are `%SECURITY-SSHD_SYSLOG_PRX-3-ERR_GENERAL` from netmiko sessions closing abruptly, peer `172.20.250.2`, then re-delivered — one event stored 1,346 times. The pipeline is mostly observing the observer, and any event count is fiction without dedupe. **Measured at T-028: the duplication is introduced by the pipeline, not present at source** — deduplicating the same device's own 200-entry buffer removes exactly **zero** records. So OBS-014 measured a pipeline artefact and generalised it to logs; a reducer tuned against that corpus would be fitting itself to this defect. **A platform fix, not a Loki limitation**: it means changing how the collector closes sessions. T-011's `run_templates` already cuts capture logins from 270 to 18, which reduces the source but does not remove it | T-004 finding (OBS-014) · OBS-027 | S | D8 |
| **B-207** | **Variance experiment** — fire the same event ten times, measure tool-sequence variance | Decides whether the agent loop is safe on top or investigations need constraining sooner than planned. **Run before Stage 2 ships, not after** | B-201 | S | D4 |
| **B-208** | **Stage 2 property suite** | The full guardrail list must hold with no human present. This is the gate on the transition, not a follow-up | B-201 | M | D19 |
| **B-209** | **Report relay hardening** — threading, per-object grouping, silence windows | A relay that posts every event unfiltered gets muted within a week | T-035 | S | interfaces.md |
| **B-210** | **Operator knowledge in the descent** | Known conditions stop being rediscovered on every run — "PE2's zero adjacencies are baked-in lab brokenness" | B-402 | S | D14 |

---

# Stage 3 — standard procedures

The write path. Everything here is gated on identity, and identity does not exist yet.

| ID | Item | Why | Depends on | Size | Decision |
|---|---|---|---|---|---|
| **B-301** | **Identity provider integration** | Verified SSO/OIDC or a signed client certificate — something a caller cannot set an environment variable to become. **Nothing else in Stage 3 starts without this** | — | L | D2, interfaces.md |
| **B-302** | **Procedure definitions** — ordered steps, preconditions, rollback plan | The human-authored artifact the model may select but never compose | B-301 | M | D2, D4 |
| **B-303** | **`propose_procedure`** — MCP tool returning a plan, never a result | Anything behind MCP is callable by the model, so MCP carries proposals only | B-302 | M | D12 |
| **B-304** | **Approval gate** | Execution triggered by a human or a deterministic policy engine reading an approved proposal — outside the model's reach entirely | B-301, B-303 | M | D2, D12 |
| **B-305** | **Execution engine** | Runs the approved procedure, verifies preconditions, executes rollback on failure | B-304 | L | D4 |
| **B-306** | **Post-action verification** | Re-run the descent after execution and confirm the finding cleared. An action whose effect is unverified is not an action, it is a hope | B-305 | M | D6 |
| **B-307** | **Write-path audit** | Who proposed, who approved, what ran, what changed, what rolled back | B-305 | M | D19 |

**The ceiling stands.** There is no Stage 4. The agent executes known procedures and hands over; it does not diagnose novel failures and act on them.

---

# Cross-cutting

Not tied to a stage. Several are cheap enough to slot into any gap.

| ID | Item | Why | Depends on | Size | Decision |
|---|---|---|---|---|---|
| **B-401** | **Juniper platform** — command lists, TTP templates, schema conformance tests · **PARKED 2026-08-16** | **Blocked: there is no Junos device or container to capture from.** Writing templates against invented output would violate the discipline that has caught most of this build's defects — six design decisions were corrected by measurement against captured reality and none by review (MVP0-REVIEW §3). A vendor's documentation is a specification, and specifications were wrong six times out of six here. **Revisit condition: a Junos device or container exists to capture from.** Nothing else unblocks it; do not start it on documentation. **The payoff test is UNVALIDATED, not passed.** D9 claims that adding a platform touches command lists, templates and tests and touches nothing above the dialect gate. That is an *argument* until a second platform proves it, and `juniper_junos` currently exists precisely to keep the abstraction honest — it shares no command words with IOS-XR — while itself being unverified. **Do not let the absence of a counterexample read as confirmation:** no code has ever run against a second vendor, so nothing has had the opportunity to leak upward, and a boundary that has never been pushed on is not a boundary that has held | **a Junos device** | M | D9 · MVP0-REVIEW §4 |
| ~~**B-402**~~ | ~~**Operator knowledge notes** — structured `notes:` per device/object~~ — **DONE (schema + seed).** `Note` on `Device`: `applies_to` (free text — the things an operator needs to speak about share no vocabulary), `note`, `author`, `recorded`, and **`revisit_when`** — the most valuable field when present, because an operator fact with no expiry condition becomes folklore and folklore outlives what it described. Seeded with **12 notes** this build actually proved: PE2/PE4's baselines learned from a broken fabric, the self-contradictory LLDP, and `bgp_no_prefixes` being normal here. `extra="forbid"` so a misspelled key is a load failure rather than a note the operator believes they recorded. **Consuming them in the descent is B-210, unchanged** | Human-authored, so pre-approved by construction. Already exists informally in `inventory/lab.yaml` comments where no code can read it | — | **S — do this early** | D14 |
| ~~**B-403**~~ | ~~**`checks.py` / `health.py` consolidation**~~ — **DONE on operator instruction (OBS-104), after being closed won't-do (OBS-101).** One module, 1,405 lines; `health.py` is a re-export shim. **Cost: T-019's acceptance criterion** — `checks.py` now imports `inventory_model` and `network_tools`, both on that criterion's declared forbidden list, so a structural guarantee became a runtime-tested one. Gained: one three-line duplicated helper. ~~Original reasoning:~~ Not deferred for risk: consolidation makes something worse whichever way it goes. They answer different questions — per-object tri-state vs per-device severity — and only **3 of 10** health rules overlap **3 of 5** checks. `checks.py` states purity as an invariant (*no inventory reads*), which is what makes a rung reproducible from a fixture; baseline rules need the inventory. `suspicious_baseline` is about the baseline and has no rung. The duplication is already guarded by an agreement test across all four labels with its own anti-vacuity companion. **Duplication guarded by a test that compares the duplicates is a redundancy with a witness, not debt** | The agreement test makes coexistence safe; merging is a later decision with real risk to a large passing suite | B-107 | M | LLD §5.2 **SHIM RETAINED 2026-08-17, reasoning recorded so nobody reopens it (OBS-123).** `health.py` is a documented re-export and **nine call sites import from it** — `mcp_server/server.py`, `cli.py`, `agent_loop.py`, `fabric_analysis.py`, `__init__.py` and four test modules. Rewriting them buys tidiness and risks a large green suite for it, which is precisely what this item warned against. **A documented re-export is one name for one thing, in the place its callers already look — that is not duplication.** All 40 rule definitions live in `checks.py`; nothing is defined twice. |
| ~~**B-404**~~ | ~~**Line accounting retrofit for `parsers.py`**~~ — **DONE (Sonnet 5, reviewed).** All six intent parsers now do §0.10 accounting: **54 ignore rules, 7 marked `NOT_NEEDED_YET`**, and `unaccounted_lines == []` across all **216** (device, label, intent) combinations. Behaviour byte-identical — records, status and every existing meta field verified unchanged against the pre-change parser on all 216; the only deltas are the two new accounting keys. Created a genuine import cycle between `parsers` and `template_parsers` (each needs the other's vocabulary), resolved by definition ordering and **pinned by a subprocess test importing each module first**, because an ordering constraint that lives in a comment is invisible where someone would break it | MVP-0 | M | BUILD-PLAN §0.10 |
| **B-405** | **Prompt library expansion** | `flow_select`, `health_summary`, and whatever MVP-1 needs. Each versioned, each with golden tests | MVP-1 | S | D18 |
| **B-406** | **gNMI telemetry as an evidence source** | Streaming counters give trend where `show` gives a point. Enrichment, never a replacement — the device wins on current state | Stage 2 | L | D8 |
| **B-407** | **Session memory** — multi-turn follow-up | Only useful at Stage 1 where a human is present. Genuinely small | MVP-1 | S | D14 |
| **B-408** | **Active probe budgeting** | Ping and TCP checks are already gated; at Stage 2 an event storm needs a rate limit, not just an on/off switch | Stage 2 | S | D13 |
| **B-409** | **Scale test** — descent against a fabric an order of magnitude larger | The tool surface and flow count are device-count independent by construction. Prove it | MVP-1 | M | Part 6 |
| **B-410** | **Runbook and on-call handover** | What an engineer does when the agent is wrong, and how they turn it off | Stage 2 | S | D3 |
| **B-411** | ⛔ **HALTED — the stated mechanism does not exist (OBS-099).** Measured: netmiko 4.7's `read_timeout` is an absolute budget that **raises**, and a raised `ReadTimeout` already yields `outputs: {}` plus a structured error. A truncated capture already yields `parse_status: failed`, so investigation reaches `unevaluated` correctly. **Nothing was implemented** — a transport guard would have been a fix for a defect that is not there. The 8-byte fixture was real; the likely mechanism is a **premature prompt match** (fast, not slow, so invisible to any timeout check), and where it still bites is **capture**: `nettools capture` gates on `errors` and not on parse status, so a truncated fixture is written and exits 0. **Recommended re-scope:** *`nettools capture` does not gate on parse status* — different file, different failure, much smaller change. ~~Original:~~ ~~A read timeout returns partial output with `status: success` and `errors: []`~~ | `_netmiko_send_commands` returns whatever arrived when a per-command `read_timeout` expires, with no error recorded — so a caller cannot distinguish "the device said this" from "we stopped listening". Measured twice: a `traceroute` to an unreachable address produced an **8-byte** file containing a single line, reported as a clean success. **Silent degradation behind a green flag is the worst failure mode in this system**, and this is its third instance — OBS-006 (MiniMax empty content with `finish_reason: length`), OBS-043 (this), OBS-044 (absent counters readable as zero). The fix belongs in the transport path: mark a timed-out command as an error rather than returning what arrived | — | S | OBS-043 · D19 **CLOSED 2026-08-17 as measured (OBS-123).** Re-measured on netmiko 4.7 with a fake raising `ReadTimeout` mid-batch: `status='error'`, one structured error naming the command, and the outputs that answered retained. The filed defect is not reproducible. **Pinned by two regression tests anyway** — "does not exist today" is not "cannot arrive tomorrow", and a retry loop that swallowed the exception would reintroduce it silently. The surviving half of the acceptance — audit every consumer of `status` — is done: a partial batch isolates to its own intent with zero commands, so no consumer reads its absence as data. |
| **B-412** | **`break_pe2_link.py` reports failure on success** | netmiko raises `ReadTimeout: Pattern not detected '[#\\$]'` on prompt detection — twice during the T-011 restore, the second time from inside `exit_config_mode` **after** the config had already committed. So the script announced failure while the fabric was correctly restored. Following the standing "retry, then halt loudly" rule literally would have meant halting while the fabric was already healthy, and against a non-idempotent action it would have re-applied a change to a device that was already correct. **Fix before any third capture window**: verify device state after a commit rather than inferring success from the absence of an exception, and treat a prompt-detection timeout as inconclusive rather than as failure. Scratchpad script, not repository code — but it is the tool that touches devices under a §0.11 waiver, which is the worst place for a misleading report. **RECURRED at T-033 (OBS-075), and the fix had been applied.** The harness was built around verify-by-reading and still raised a manual-intervention alarm on a healthy fabric, because the verification ran only on the success path and a `TypeError` took the error path. Reopened and widened: the requirement is now *verification on every path including the error path*, plus **no broad `except Exception` around anything but the device call itself** — the original exception was a code defect rendered as a network failure. Still open, and now blocking any further device-touching run | — | S | OBS-049 · OBS-075 · §0.11 |
| ~~**B-413**~~ | ~~**`TROUBLESHOOTING_PROMPT` carries the banned self-evaluation clause**~~ — **DONE.** And there were **three**, not one: `fabric_analysis` and `agent_loop` carried it too. Replaced with a constraint (*if a claim cannot be traced to a specific command's output, do not make it*), and pinned package-wide so a fourth is covered without anyone remembering | `llm_analysis.py`'s prompt ends with *"Before responding, verify that every claim is supported by the provided data."* — the exact clause `prompts/README.md` explains is not what makes output trustworthy here, and that `tests/test_prompts.py` now bans from any prompt in the library. It reads as reassurance and provides none: evaluation is `grounding.py` and the schema validator, both of which run every time without anyone's attention, which is a property a prompt clause cannot have. Harmless today — that prompt predates the library and is not covered by its rules — but it should not sit indefinitely contradicting the README. **Remove when that prompt is next touched**, which is a version bump under the library's own rules. | — | S | OBS-059 · T-026 |
| **B-414** | **Log normalisation as a general capability** | `evidence-reduction.md` §3 reductions 3 and 4: group records by `(mnemonic, normalised body)` and collapse each group into one record carrying count, first/last seen, rate, subjects, and **one verbatim sample** — the receipt an engineer checks and the grounding check cites. On IOS-XR the mnemonic already *is* the template key and the parser already emits it, so this is a `Counter` and a body normaliser, not a clustering problem; reserve Drain-style clustering for genuinely unstructured sources. `log_window.shape_window` is the first instance and covers reductions 1, 2 and 5 for a single device's `show logging` buffer — deliberately not 3 and 4, because at 28 records the aggregate carries no information the records do not and collapsing them costs the verbatim ordering the timeline is built from. **B-206 is what makes it necessary**: a Loki-backed window is cross-device and time-unbounded, where counts and rates *are* the evidence. Two properties must hold on day one — no minimum-count threshold (a singleton among thousands is frequently the answer, pinned today by `test_a_singleton_survives_a_window_of_thousands_of_repeats`) and distinct mnemonics never sharing a template ID | B-206 | M | evidence-reduction.md §3 · D8 |
| **B-415** | **Clock-skew detection across devices** | `evidence-reduction.md` §7, failure mode 3. Multi-device correlation orders events by the device's own timestamp (never ingest time, §4), which is correct and which breaks silently when two device clocks disagree — the resulting timeline is internally consistent and refers to the wrong ordering, so nothing downstream can detect it. Detect and report the disagreement as a finding rather than ordering through it; a skew beyond threshold means clock drift, collector backlog, or a dropped batch, all of which are worth knowing. **Untestable today for an honest reason**: every window this layer reads is single-device, so there are no two clocks to disagree. Scope it into the same change as B-206, which is what introduces the second clock | B-206 | S | evidence-reduction.md §7 |
| **B-416** | **Event episodes — deterministic sequence construction** | **Not cosmetic — do not descope this as rendering.** OBS-078 upgraded it from presentation to *mechanism*: two independent faults rarely land in the same instant, so a **second configuration commit in the window, unexplained by the localised cause**, is the timeline signature of a masked second fault. Since the rung tables of a one-fault and a two-fault incident are byte-identical (D6, chaos-harness §7), the signal cannot come from the descent — and the timeline is one of only two places it can come from. An episode is a time-bounded sequence of events related by device, topology object, protocol dependency and temporal proximity. Independent aggregation destroys causal chronology, and the ordering is frequently the strongest evidence in the window: six events aggregated separately become six records with `count: 1`, all true, with the thing that identifies the cause gone. **An episode is the log-side mirror of the dependency descent** — the descent walks *down* the stack asking "is this layer healthy", the episode is that same stack observed propagating *upward* in time, and agreement between them is corroboration neither axis produces alone. The measured `broken` window contains a real six-event episode; note that **two of the descent's five rungs have no log event at all**, so an episode is evidence *for* a descent and never a substitute. **Measured design constraint:** the interface→BGP gap is **154 seconds** — the hold timer, not processing delay. A proximity threshold of a few seconds splits that episode in two and severs exactly the link it exists to preserve, so the bound must be derived from **protocol timers** (BGP hold 180s, IS-IS 30s), never from intuitions about "at the same time", and the bound used must be recorded | B-414 | M | evidence-reduction.md §6 · OBS-067 |
| **B-417** | **Relationship-aware projection over a bounded topology neighbourhood** | Projection by identifier match is too narrow and measurement proves it: subject filtering retains **0 of 28** records on the `broken` window. The generalisation is an evidence neighbourhood — depth 0 the subject, depth 1 the interfaces and adjacencies its session depends on, depth 2 the adjacent devices those terminate on. The measured case lands at **depth 2** (the events are on PE2 and name P1/P3, for a session RR1 holds with `10.255.0.12`). Resolution is arithmetic over inventory and topology, deterministic, never a model inference, and belongs beside the flow registry with the `DeviceScope.SUBJECT`/`PATH` resolver. **Depth is bounded and declared** — an unbounded neighbourhood is the whole fabric, and recording the depth is what makes an empty result at depth 1 distinguishable from one at depth 3. **Caution:** this fabric's LLDP is self-contradictory (`learn-topology` reports it), so derive the neighbourhood from declared inventory topology and treat LLDP disagreement as a reason to widen rather than to pick a side | B-107 | M | evidence-reduction.md §5 |
| **B-418** | **Temporal shape — `max_rate_1m`, burst detection** | Two records with identical counts can mean opposite things: 60 events evenly spread over an hour is a chronic condition, 60 in ninety seconds is an incident. Preserve `max_rate_1m` and a burst count alongside the total. Same reasoning as the interface error-counter check being a rate rather than a total, and it is what distinguishes a dying optic from a historical incident. Small once B-414 exists, and meaningless before it | B-414 | S | evidence-reduction.md §3.4 |
| **B-419** | **`expand_evidence` and the four evidence tiers** | Reduction is mandatory and outside model control; **investigation scope is not**. The model never gets `get_raw_logs()`, but it may get a bounded drill-down returning first occurrence, last occurrence, neighbouring raw records and a storage pointer for one evidence key. Four tiers, each reachable only from the one above: 0 the finding and its chain, 1 normalised records, 2 representative verbatim samples, 3 raw records via `expand_evidence` only. Passes D11's tool test — the model genuinely must decide when it needs more proof — and it removes the incentive to send everything "just in case". **The defensible exception** to the five-tool decision; the other six tools revision 2 proposed collapse into existing tools with different `intent` values | B-101, B-414 | M | evidence-reduction.md §9 · D11 |
| **B-420** | **Coverage metadata, and grounding enforcement of absence claims** | **Pulled forward — see T-029a. Lands before T-032.** Grounding enforces citation for claims of **presence** and nothing for claims of **absence**, so "no correlating events in window" passes today with nothing behind it, on a source measured to drop severity 5 and 6. Every evidence response carries coverage (`source`, `query_complete`, `buffer_wrapped`, `severity_available`/`severity_missing`, `records_kept_unattributable`, `device_clock_skew_ms`), and a claim of absence without coverage backing it fails grounding. **The measured case is worse than an empty result**: querying the log platform for PE2's window returns two real, correctly-timestamped severity-3 link events from **the previous day's restore** and nothing from the isolation — a timeline that is plausible, internally consistent, and about a different incident (OBS-067). This is the same asymmetry `check_chain_coverage` exists to close, on a different axis: **a check that inspects only what is present cannot see what was omitted** — hence a peer check, since the coverage record is not in the report. Raised in stakes by `correlate.v2`, whose refusal case is now nine irrelevant records rather than an empty list | — | S | evidence-reduction.md §7 · T-029a |
| ~~**B-421**~~ | ~~**Prompt caching is not applied on the rendered-prompt path**~~ — **DONE (Sonnet 5, reviewed and corrected).** `RenderedPrompt(system, user)`; the split is done **in code**, so no prompt version bump was needed and `prompts/` is untouched. Anthropic gets the static half as one `cache_control: ephemeral` block; OpenAI/MiniMax/Ollama concatenate and behave exactly as before. `TokenUsage` gained `cache_read_input_tokens`/`cache_creation_input_tokens`. **Cacheable prefix: report 4,432 chars, correlate 6,446.** *Corrected on review:* the first version moved payloads without their headings, leaving section headings promising content that had gone and three anonymous JSON blobs in the volatile half — the association between a heading and its payload is content, so each payload now carries its heading, at a cost of <100 uncached chars | `llm_analysis.complete_prompt` sends a fully-rendered prompt as one user message. Anthropic's cache is a **prefix match on `system`**, and a rendered prompt arrives with its static template and volatile payload already interleaved, so nothing is cacheable. Splitting it apart means `build_report_prompt`/`build_correlate_prompt` returning two pieces (static system block, volatile message) rather than one string, which is a prompt-library change and not CLI wiring. The prompts are a few kilobytes, so this is **cost, not correctness** — and it grows with investigation volume, which is the point at which it becomes worth doing | T-031 | S | OBS-073 |
| ~~**B-422**~~ | ~~**`--format json` is not directly pipeable**~~ — **DONE.** `_note` writes to **stderr**; stdout carries the payload. The ordinary Unix split — stdout is data, stderr is about the run. A human sees both interleaved exactly as before; a pipe gets only the payload. Three existing tests asserted notes on stdout and now assert them on stderr, which is the same property on the right stream | `_note` prints informational lines prefixed with `#` to **stdout**, so `nettools investigate ... --format json | jq` fails on the note line. A repo-wide convention rather than an `investigate` defect — `nettools diff` does the same for its snapshot path — which is why it was not changed inside T-031. The fix is to route `_note` to stderr, which is what it is: informational output, not data. Small, and it touches every command's output tests | — | S | T-031 |
| ~~**B-423**~~ | ~~**CI job: run `--from-fixtures` in a clean environment**~~ — **DONE.** `offline-demo` job: installs **without the `llm` extra** (so an accidental module-level provider import fails there rather than passing on a machine that has it), asserts no `.env` and no credentials exist, then runs all three labels and checks all three exit codes plus JSON consumability. Network isolation via `unshare -rn` is **detected and announced**, never assumed — a job that silently stops isolating is the degradation shape this build removes; the hard no-network guarantee remains `test_end_to_end_offline.py`'s raising netmiko. Every assertion dry-run locally in an empty environment before committing | A container with **no `.env`, no `DEVICE_*`, no API key and no network**, running `nettools investigate RR1 10.255.0.12 --from-fixtures` and asserting exit 1 and the causal chain in the output. **The offline demo's entire value is that it needs nothing, so nothing is the only environment that tests it.** The in-suite test (`tests/test_cli_investigate.py`) strips the environment with `monkeypatch`, which catches the code path but not a future dependency on a file, a socket, or a package that happens to be installed on a developer's machine. This is the **setup** face of §0.13 given a permanent guard: the failure it prevents is invisible from inside the repository, so the fix has to be an environment rather than an inspection. Cheap — one job, no services, and it is also the fastest signal in CI | — | S | §0.13 · OBS-072 |
| ~~**B-424**~~ | ~~**The correlation path has no citation check**~~ — **DONE, T-029b (OBS-081)** | `ground_correlation` runs `check_absence_coverage` only, which returns a **vacuous pass** whenever `found` is not `False`. A correlation asserting *presence* is therefore emitted with zero verification. Measured live at T-033: the model emitted `Aug 14 04:28.238 UTC` for a record whose real timestamp is `Aug 16 14:04:28.238 UTC` — a malformed date two days earlier, in the one field `correlate.v3` constraint 2 says to quote exactly — and **1 of 9 timeline timestamps did not exist in the window**. Grounding passed it. The exact mirror of the gap T-029a closed: the report checks presence and (n/a) absence; the correlation checks absence and **not** presence. The fix is already specified by the existing code — every timeline entry's `at` must be a timestamp present in the shaped window and its `mnemonic` must match the record at that timestamp, which is `check_grounding`'s evidence-key rule applied to the other output. **Recommended as T-029b, before T-034**, mirroring B-420 → T-029a | — | S | OBS-077 · T-029a |
| ~~**B-425**~~ | ~~**Token usage is not instrumented on the rendered-prompt path**~~ — **DONE.** `TokenUsage` + `Completion`; `complete_prompt` returns both. Carried on `InvestigationResult`, in the payload, and rendered. **Measured live: 1 call, 2,286 tokens (1,840 in / 446 out)** for a healthy investigation — the character proxy T-033 had to report was ~2× that, which is why an estimate should never have been offered as a measurement. `reported=False` distinguishes *the provider did not say* from *it cost nothing*, and one unreported call taints a total | `complete_prompt` returns text only; neither the Anthropic nor the OpenAI/MiniMax route surfaces `usage`, so T-033 could report only a proxy (~5k tokens of prompt per investigation — 6,673 chars report + 13,218 correlate — response excluded). Cost per investigation is a number this project will be asked for, and estimating it from character counts is the kind of measurement that quietly becomes folklore. Return usage alongside the text and thread it into `InvestigationResult`. Pairs naturally with **B-421** (prompt caching), since caching is only worth doing if its effect can be measured | — | S | OBS-077 · T-033 |
| **B-426** | **Fault injection harness — and the Stage 2 acceptance vehicle** | **Larger than a normal item; see the block below the table.** An injector that is **not the agent and shares no context with it**, so a trial is blind by construction rather than by discipline | B-201 · four manual rounds first | L | D6 · D19 · OBS-078 |
| **B-427** | **Evaluation corpus and confusion matrix** | **BINDING: corpus integrity.** > *A corpus records what the system did at the time, not what it does now. Scores are never rewritten after a fix. A corpus that rewrites its own history cannot demonstrate that a fix worked, which is most of what a corpus is for. Corrections are appended as new rows with the fix referenced.* Round 4 is the worked example: it scored ❌ on diagnosis, B-428 closed that class, and **row 4 keeps its ❌** with a new row appended referencing B-428. The rule is binding rather than a judgement about that row, because the pressure to tidy a corpus is strongest exactly when a fix has just landed and the old score looks like an embarrassment rather than evidence. `tests/test_rounds_regression.py` pins all four vectors executably alongside three corpus shapes, which is the form a row should take. **Score more than the rung.** Round 3 (OBS-092) is the worked example: the rung matched, and a matrix scoring rungs alone would have recorded a clean pass on a round that was weaker in two separate ways. Required columns: **(a) predicted rung vs actual rung**; **(b) whether the match came via the primary prediction or a declared refutation branch** — round 3 matched via the branch, which is weaker evidence than rounds 1 and 2 and must not be averaged in with them; **(c) "answer weaker than evidence supported"** — round 3 reported `transport_blocked` while the collected, parsed envelope contained `last_reset_reason: administrative shutdown`, so the answer was correct and impoverished. **A correct-but-impoverished answer is a real cost and is currently unscored** (silent-failure shape 7, §0.13). Column (c) cannot be graded from the output — it needs the envelope beside it. Predicted rung vs actual rung across trials, as a matrix rather than a pass rate: *which* rung the descent lands on when it is wrong is the whole diagnostic signal, and an aggregate score discards it. Cluster failures by rung, by device, and by fault class — a descent that is reliable on `interface` and unreliable on `transport` is a different problem from one that fails on PE4 specifically, and both look like "87%". **Re-runnable after every change, as a regression suite**, which is what turns one-off trials into a corpus. Grows from the manual rounds: each round is a row before the harness exists, and the harness only automates the row-producing. **BINDING CORPUS REQUIREMENT (OBS-089).** > *A trial tests resolution only if the wrongly-resolved device differs in observable state from the correctly-resolved one. A corpus that does not arrange this reports resolution as passing while measuring nothing — §0.12's vacuity, in the evaluation corpus rather than in a test.* Round 2 is the worked example and it is a **corpus-design defect, not a system defect**: the subject resolved correctly to PE1, but the cause sat at rung 2 (`LOCAL` scope, no resolution needed) and rungs 4–5 resolved to a device whose state was identical to the alternative. A resolver error would have produced a byte-identical result. The round scored a pass on resolution and tested nothing about it | B-426 | M | D19 · OBS-078 · OBS-089 |
| ~~**B-428**~~ | ~~**Forward consistency — the descent has no mechanism for concluding health**~~ — **DONE (OBS-097).** `no_fault_on_path`, exit 0. Clause 2 of the original specification was dropped after a HALT (Q-020/OBS-096); the shipped rule is clause 1 alone | > **A cause with no chain is a cause explaining nothing.** **This is a missing half, not an enhancement.** The walk exists to find the lowest broken thing. *"Nothing that matters is broken"* is the one answer it has no mechanism for reaching — so the true-negative case is where a dependency-descent architecture is **structurally** weakest (chaos-harness §4.3), and forward consistency is the half that is absent rather than an addition to the half that is present. Two-fault detection is a **side effect** of having it, not the reason for it. Concretely: after localising, check that the observed upper rungs match what the cause **alone** predicts. The degenerate instance is *"there is nothing to explain"* (rung 1 healthy); the two-fault instance is *"predicted a timeout, observed an admin shutdown"*. Same check. **Measured offline (OBS-079):** one uplink down on a redundant device gives rungs 1–4 healthy, rung 5 broken, and the descent reports `cause: interface` with an **empty causal chain** — a fault localised on a session that is Established and carrying traffic. The empty chain is the tell and `DescentResult` already carries it; nothing reads it. This is also **OBS-057's stated revisit condition for `EACH_PHYSICAL_INTERFACE` + `ALL_HEALTHY` arriving** — and **weakening the aggregation is not the fix**, since that would lose the isolation case the rule exists for, where all uplinks down genuinely means isolated. Forward consistency keeps both. **DEMONSTRATED ON LIVE HARDWARE, round 4 (OBS-094).** Nine of nine predicted values matched, no falsifier fired: BGP Established and carrying traffic, `cause: interface on PE2`, empty causal chain, `trustworthy: true`, **exit code 1**. Two things the round settled. **(a) The information is sufficient** — MiniMax, reading only the descent payload, wrote *"because higher layers are healthy, the broken interface state observed here is not on the dependency path"* and recommended clarifying scope. The model performed this check unprompted and correctly, so the fix is a **predicate, not an evidence-gathering exercise**. **(b) The prose is right and everything a machine consumes is wrong** — the model's caveat lives in the layer this architecture deliberately treats as non-authoritative, while the finding, the payload and the exit code all assert a fault. **No longer a hypothesis to validate; a measured defect to implement** | — | M | OBS-079 · OBS-057 · D6 · chaos-harness §4.3 |
| ~~**B-429**~~ | ~~**A vacuous pass over a payload full of claims should be an error**~~ — **DONE, T-029c (OBS-083)** | `GroundingResult.vacuous` exists (§0.12) so a pass over nothing is distinguishable from a real pass, and it worked: the T-033 output printed `vacuous pass -- 0 observations, 0 citations` **next to a nine-entry timeline**. The instrument reported the gap in every payload and nobody read it. A distinct failure from §0.13's family — not *the evidence could not have shown you*, but **the evidence did show you and nobody looked** — and a guard reporting honestly into an output no one reads is, in effect, not running. The fix is cheap: where a result is `vacuous` and the graded payload plainly contains claims (`timeline` non-empty, `observations` non-empty), that is a contradiction the code should raise rather than a note a reader should notice. Generalises beyond grounding to any self-reporting guard | — | S | OBS-081 · §0.12 |
| ~~**B-430**~~ | ~~**`bgp_transport` ignores `last_reset_reason` — the answer is already in the record**~~ — **DONE.** Appended to the broken-transport reason, **marked as history and dated with `last_reset_ago`** so staleness is the reader's to judge. It never becomes a verdict: measured across this corpus, 14 of 16 sessions carrying a reset reason are **Established**, so promoting it would trade a shape-7 under-report for a confident wrong answer. The field audit (B-433) caught the move in both directions on its first real use, one commit after being built | The check reads `meta["connection_state"]` and nothing else. Measured live at round 3: RR1's own `show bgp neighbor 10.255.0.12` parsed to `connection_state: Active` **and** `last_reset_reason: "BGP Notification received: administrative shutdown"`. **The far end told RR1 exactly why the session was down, the parser captured it, and the check ignored it.** The hand diagnosis reached the same conclusion by logging into PE2 and reading `Idle (Admin)` from the other side — which was not necessary. Not a wrong answer: `transport_blocked` is true and the recommendation written from it is sound. A **wasted** answer: the descent had the material to say *"the peer administratively shut this session"* and said *"the transport is not established"*. Strongest single argument for **B-428**, since "what do the upper rungs say about *why*" is exactly what that field answers. One care point — `last_reset_reason` is history, not current state, so it must qualify a finding rather than become one on its own | — | S | OBS-092 · B-428 |
| ~~**B-431**~~ | ~~**`EACH_PHYSICAL_INTERFACE` is a naming filter, duplicated three times, and empty raises**~~ — **DONE.** `interface_kind.py`: one declared table, six kinds, `UNKNOWN` surfaced rather than defaulted to non-physical. All three sites converged; `fixtures.py`'s deliberate difference (ports **and** `Lo0`) is now written against the shared predicate so it reads as an exception rather than a third rule. Empty member set is `unevaluated` — not the `IndexError` it was, and not the vacuous `healthy` that `all([])` would give | The rule is `name.startswith("Gi") and "." not in name`. Round 3 confirmed it excludes `BV200` correctly — **and for the wrong reason**: because the name does not begin with `Gi`, not because anything knows a bridge-group virtual interface is not a physical member. On this fabric every physical interface is a `GigabitEthernet`, so the rule is right **by coincidence of naming**. Three consequences this fabric cannot show: a `TenGigE`/`HundredGigE`/`Bundle-Ether` interface is **silently excluded from the rung that matters most**; if that empties the member set, `descent._aggregate` raises **`IndexError`** from `results[0].subject`, uncaught by `_cmd_investigate`'s `(ValueError, KeyError)`, surfacing as a traceback in the emit path; and the filter is duplicated in `descent.py:166`, `investigation.py:312` and `fixtures.py:152` — the last with a *different* definition. **The duplication is the more dangerous half.** A filter defined three times is **three filters**: they agree today by accident of nobody having edited one, and the first divergence will be a descent that collects one member set and aggregates over another. That failure reports a plausible verdict over the wrong members and is invisible to every check in the build. Fix: one declared predicate, semantic rather than nominal, plus an explicit empty-set verdict (`unevaluated`, not a crash and not a vacuous `healthy`) | — | S | OBS-092 · OBS-057 · §0.12 |
| ~~**B-432**~~ | ~~**`cause_not_localised` is unreachable for `bgp_session` as specified**~~ — **DONE, option A, and cheaper than either option as written.** No new command and no `VERB_ALLOWLIST` change: the TCP-layer signal was **already in `show bgp neighbor`** and was sitting in two declared `IgnoreRule`s labelled *"socket bookkeeping"*. `Socket ... armed for read` is a different subsystem from the BGP FSM, so rung 2 can now disagree with rung 1 and `cause_not_localised` is reachable — an AS mismatch, a capability mismatch or an MD5 failure after TCP establishes all produce that shape. Measured: armed on **14 of 14** Established sessions, not armed on **2 of 2** Idle ones, and a test pins that discriminator | Rung 1 reads session state from `show bgp summary`; rung 2 reads `connection_state` from `show bgp neighbor`. **Two independent commands reporting the same FSM.** `cause_not_localised` needs rung 1 broken with every rung beneath it healthy — here that requires the summary to say not-Established while the neighbour detail says Established, which is not a fault state but an inconsistent device. So one of the five findings the flow declares is dead in practice, and `report.cases.json`'s golden case is composed **because it has to be**. Round 3 was designed to capture it from a real fabric and produced `transport_blocked` instead (OBS-092). T-027 said "no consistently-behaving fabric can produce this"; OBS-080 corrected that to "a fact about single faults"; the accurate version is narrower — **it is a fact about rung 2 restating rung 1's state machine.** A rung 2 testing something genuinely independent (reachability by probe, a TCP connect check) would reach it. **The design consequence is larger than the finding: two rungs reading the same state machine are not two rungs.** Both options recorded, neither resolved. **(A) Rung 2 tests something rung 1 cannot see.** An actual TCP probe to port 179 — which is an **active probe with its own allowlist class**, and `VERB_ALLOWLIST` is currently `{show, ping, traceroute}`, so it is a change to the safety boundary and must be treated as one, not as a check improvement. *Worth evaluating first: a passive middle ground may dominate it* — `show tcp brief` reports whether a TCP session to 179 exists locally, needs no new verb, and is genuinely invisible to rung 1. If it suffices, option A costs nothing. **(B) The `bgp_session` flow has four rungs, and should say so.** Delete rung 2, delete `transport_blocked`, and state in the flow that transport is not independently observable from `show` output. Honest, cheaper, and it removes a finding the corpus has now produced twice. The cost is that `transport_blocked` was the *right* answer in round 2 — a genuine transport fault with a healthy underlay — so option B collapses two distinguishable real faults into one. **Do not resolve without the round-2 and round-3 envelopes side by side** | — | M | OBS-092 · D6 · T-027 **LEADING HYPOTHESIS 2026-08-17, promoted from an open question.** Rung 2's observable is a **prerequisite** for rung 1's — TCP up is necessary for Established — so no *stable* fault separates them, and the separating states (`OpenSent`, `OpenConfirm`, the retry window of a rejected OPEN) last seconds. **So the answer may be that `bgp_session` has FOUR rungs, not five**, and that if rung 2 is to stay it needs an observable that is **not downstream of rung 1's** — something that can be broken while the session is up and healthy while the session is down. The socket is not that; it is the same causal chain one layer lower. Round 8 (AS mismatch) tests whether the transient separation is observable at all; a negative there is evidence for the four-rung reading rather than against it. |
| ~~**B-433**~~ | ~~**Audit what every check reads against what its inputs contain**~~ — **DONE.** `tests/test_field_audit.py` holds the split as pinned numbers (`bgp_neighbor` 5/23, `interface` 5/14, `route` 2/8) and enumerates the **explanatory** fields separately — the ones whose whole purpose is to say *why* something is in a state, which is the population shape 7 is drawn from. Fails when a parser gains an unread field **or** when a check starts reading an explanatory one, so either direction is a decision rather than a drift. Deliberately **not** a rule that everything must be read; a test asserts that boundary | The detection method for silent-failure **shape 7** (§0.13), and mechanizable rather than a discipline: enumerate the fields each parser emits from a real fixture, grep the check module for each. Measured today — `bgp_neighbor` **5 of 23** fields read, `interface` **5 of 14**, `route` **2 of 8**. **Not all 33 unread fields are defects**; `mac_address` and `bandwidth_kbps` are not diagnostic, and treating every unread field as one would reproduce exactly the noise-generating over-correction T-029c refused. But `bgp_neighbor`'s unread list includes `state_reason`, `previous_state`, `remote_as`, `hold_time` and `keepalive` — **an AS mismatch and a timer mismatch both produce the same `Active` state round 3 produced, and the tool cannot distinguish either from an administrative shutdown while holding the fields that would.** Deliverable is the audit and its output as a committed artefact; deciding which fields are load-bearing is per-check judgement and belongs with whoever owns the check. Run it as a test so it fails when a parser gains a field nothing reads | — | S | OBS-092 · §0.13 shape 7 |
| ~~**B-434**~~ | ~~**Review §0.10's ignore rules for diagnostic value**~~ — **DONE.** `IgnoreKind` on every rule: `NO_EXTRACTABLE_FIELD` (permanent — a section header, a capability flag) or `NOT_NEEDED_YET` (real content, no consumer yet). **74 rules reviewed, 15 deferred**, pinned by count and spot-checked in both directions so neither kind becomes a dumping ground. The deferred set is the diagnostic one: duplex/speed, link reliability, slow-peer state, prefixes-denied counters, connection flap counts, interface traffic rates, BFD state, configured-vs-negotiated timers | B-433's audit compares *parsed* fields against *read* fields. It cannot see a line that was **never parsed** — and B-432's TCP signal was exactly that, sitting in two `IgnoreRule`s labelled *"socket bookkeeping"* on every device and every label. **§0.10's ignore rules are shape 7's other reservoir.** Enumerate every `IgnoreRule` across the parsers, and for each record whether it was skipped because the line carries no extractable field, or because nobody needed it yet — those are different decisions and only the first should be permanent. Cheap: the rules are already declared, named and reviewable, which is why B-432 took one grep. An undeclared regex would have swallowed it invisibly | — | S | OBS-100 · §0.10 · §0.13 shape 7 |
| **B-435** | **`topology.py` compares LLDP device IDs against inventory labels, not configured hostnames** | The anomaly report has classed three known devices as *"LLDP neighbours not in this inventory"* and one real agreement as a *"link where the two ends disagree"*, for the whole life of the project. Neither is true: at `t0`/`t1` P1 was configured `LEAF05_DHCP_SERVER`, P3 `Lab-leaf01`, PE4 `SDWAN-Edge01`, and LLDP reported those names correctly (OBS-103). The fix is a mapping — the `facts` intent already parses each device's configured hostname, so `topology.py` can resolve a device ID to an inventory name before comparing. **Do not delete the anomaly classes**: a genuine LLDP disagreement and a genuinely unknown neighbour are both still worth reporting, and after this they would mean it | — | S | OBS-103 · Phase 3 |
| **B-436** | **Evidence-epoch contract — collect once per device, reuse across rungs** | **Reviewers A + B + C, `peer-review-response.md` §2.1, §3.4.** The convergent finding, and the highest-cost defect. Each rung calls `collect_evidence()` afresh; a five-rung descent spans 114–122 s across multiple sessions and devices, and the walker discards each collection's timestamp and treats the verdicts as one state. **During convergence those states may never have coexisted.** A's scenario: a fault recovers at t50, an unrelated interface fails at t105, the interface rung reads it at t115, and the assembled vector makes the new failure explain the earlier outage — every citation resolving, grounding passing, the model rendering fluent causal prose. The rule, correctly scoped since CLI cannot give atomicity: *a causal finding may be asserted only when the observations have a bounded, recorded skew and the symptom and proposed cause remain stable across the observation interval.* Where that cannot be established the answer is **`temporally_incoherent`** — the same shape as `no_fault_on_path`. **One change, three defects**: also A's §4.6 device load and B's §3.4 speed objection, the latter being a correctness argument (an operator runs these five checks faster by hand) | — | M | review §2.1, §3.4 · A+B+C |
| **B-437** | **Rung discrimination criterion, binding on every future rung** | **Reviewer A, §2.2.** Rungs are currently defined by available CLI views rather than by independently falsifiable dependency hypotheses. A valid rung must carry: a dependency assertion; an observation from a **distinct subsystem**; at least one sealed case where the rung above is broken and this one healthy; at least one where this one is broken and the rung below healthy; and the expected upper-layer signature if this rung alone is the cause. B-432 gave rung 2 a distinct subsystem but no captured fault yet separates it | — | S | review §2.2 · A **CLOSED 2026-08-17, at its measured size rather than its filed one.** Audited against the five rungs that exist before building to the spec. **Part 2 (distinct subsystem): satisfied for all five**, and pinned mechanically — no two rungs draw from an identical set of collect steps, which is the checkable half of it. **Parts 3/4 (separating case): 3 of 4 boundaries have captured evidence.** The one gap is `bgp_session|transport`, separated only by the composed `BHHHH` vector — which assumes what it would need to prove — and that is exactly the gap B-432 predicted. Round 3 was meant to close it and produced `BBHHH` instead, because the admin shutdown broke the socket too. Filed as **B-463**. **Part 5 (upper-layer signature): deliberately not built** — it is forward consistency, which is **Q-019**'s open question, and two items answering one question badly is worse than one answering it later. So what remains of B-437 is a rule on `Rung` plus `test_rungs.py`, which makes it **binding on future rungs by failing** rather than by being remembered — and that was the load-bearing half of the filed item all along | — | S | review §2.2 · A |
| ~~**B-438**~~ | ~~**`pin_lab_golden_snapshot` performs a persistent write behind `_read_only_tool`**~~ — **DONE, and it was three writes not one.** `save_lab_snapshot` and `pin_lab_golden_snapshot` removed; **both diff tools also persisted** their fresh collection, which the review did not name, and snapshot history is what `detect_lab_flaps` reads. Guarantee is now structural: the module does not import `save_snapshot` or `save_golden_snapshot` at all, so no tool it exposes can reach one — a decorator's name replaced by the import graph. Pinning stays a human action (`nettools baseline pin`), asserted by a companion test so scoping the capability did not delete it | **Reviewer A, §3.1.** A model can pin an outage state as golden, after which drift comparison suppresses that fault indefinitely; it can also alter the snapshot history flap detection reads. Contradicts **D12** (execution is never behind MCP) and **D14** (memory is derived, never authored). > *"The architecture protects the managed network more carefully than it protects its own source of truth."* Small fix, active hole | — | S | review §3.1 · A |
| **B-439** | **Render the authoritative report deterministically from typed fields** | **Reviewer A, §3.2.** `check_grounding` proves citation *topology*, not truth: a report can cite an interface-down key while asserting a chassis power failure and pass. Deterministic entailment over natural language is not available, so the response is structural — the authoritative report is generated from typed fields and any model paraphrase is marked non-authoritative. **Deferred until B-436 lands** (§5). See **B-453** for the intermediate check available now | — | M | review §3.2, §5 · A |
| **B-440** | **Round 6 — a real BGP fault alongside an unrelated down physical interface** | **Reviewer B, §3.3**, the trust-loss scenario, and it describes this fabric: PE1 and PE3 carry a permanently line-down `Gi0/0/0/2.300`. B-428 catches the healthy-rung-1 case; an unrelated down *physical* interface beside a real BGP fault produces the same vector and is untested. > *"This is how operational tools lose adoption: not through a dramatic outage, but by wasting one bad night with a confident, specific, plausible answer."* Requires B-431 (**done**) first | B-431 ✓ | M | review §3.3 · B |
| **B-441** | **Report all three strata, and name the sampling frame** | **Reviewer C, §3.5, and it corrects a decision I made during the four rounds.** Refusing to report 3/4 was endorsed on the grounds that the denominator averages a working category with one that structurally could not. C's rationale is better: *"Deployment performance necessarily combines categories. The appropriate combination depends on their production prevalence, which is currently unknown."* Report 3/3 on-path, 0/1 no-fault **and** 3/4 overall, stating that none estimates field accuracy **because the cases were not sampled from a defined population**. The problem was never the denominator — it is the sampling frame | — | S | review §3.5 · C |
| **B-442** | **`chaos-harness.md`: three datasets, estimand plan, stratum sizing** | **Reviewer C, §3.6 + §3.7.** §3.5 says the holdout validates fixes; §11 says re-run it as a regression suite. Incompatible: *"once its results influence a fix, model choice, parser or prompt, it is a regression set."* Three sets — **development** (visible), **regression** (visible after first failure), **one-shot audit** (inaccessible, evaluated once on a frozen release, then spent). Sizing derives from the claim: **59** trials per stratum for a one-sided 95% upper bound on critical error below 5%; **299** for below 1%; ~**97** for ±10 points; ~**385** for ±5. With C's caveat: *"Fifty-nine repetitions of one tool-aware true negative do not bound the false-positive rate on production incidents. Sampling validity comes before sample size."* **See the HALT on B-452** | B-452 | M | review §3.6, §3.7 · C  **CLOSED 2026-08-17 under the narrow fix (OBS-105).** §3.5 rewritten: development and regression written in full, the one-shot audit set **specified and explicitly not established**, with both live leakage routes named. §11's phase list gains phase 9 blocked on B-452, and the phases that previously named the same set for validation and regression are separated — that was the incompatibility. Sizing recorded with C's caveat as the operative sentence: sampling validity before sample size, so the numbers are the last thing to act on and this project has trials and no estimand until B-452 lands. |
| **B-443** | **BGP object identity: `(device, instance, bgp-instance, peer, afi-safi)`** | **Reviewers A + B, §5.** Deferred until a second address family or a VRF-scoped session exists | — | M | review §5 · A+B |
| **B-444** | **Passive-read admission control and fan-out limits** | **Reviewers A + B, §5.** Deferred until any multi-device concurrent deployment | — | M | review §5 · A+B |
| **B-445** | **Both-end session evidence** | **Reviewer B, §5.** Deferred until escalation-grade output. Note B-430 (**done**) already surfaces the far end's stated reason from the local device | — | M | review §5 · B |
| **B-446** | **Ticket-grade run bundle and handover view** | **Reviewer B, §5.** Deferred until workflow adoption | — | M | review §5 · B |
| **B-447** | **EVPN/SR object cardinality** | **Reviewer A, §5.** Deferred until either flow is designed | — | M | review §5 · A |
| **B-448** | **Vendor semantic equivalence** | **Reviewer A, §5.** Blocked on hardware — see **B-401**, parked for the same reason | B-401 | M | review §5 · A |
| **B-449** | **Service-level forwarding validation** | **Reviewer A, §5.** Deferred until any L3VPN or EVPN claim is made | — | M | review §5 · A |
| **B-450** | **Operator utility study** | **Reviewer C, §5.** Deferred until a characterised failure envelope exists | — | M | review §5 · C |
| **B-451** | **Round 5 — invoke during propagation, not after** | **Reviewers A + B + C, §6 item 4.** The test that proves B-436. B independently proposed it; C's version is that the protocol *"can never establish correctness while the network is changing, no matter how many settled trials are run."* **One piece of work with B-436** | B-436 | S | review §2.1, §6 · A+B+C |
| **B-452** | **Frozen-release audit governance** | **Reviewer C, §5 — and I am flagging this deferral as wrong; see the HALT recorded at OBS-105.** §3.6 defines a one-shot audit set whose defining property is that it is *inaccessible to developers and spent after one evaluation*. That property has no mechanism without governance, and §3.6 itself names **two leakage routes that apply today**: the same tool-aware person defining both catalogues, and **local Git history treated as an immutable seal when it can be amended or rebased**. Deferring the governance while specifying the dataset produces a set that is one-shot in name only | — | M | review §3.6, §5 · C |
| **B-453** | **Identifier containment — the intermediate grounding check available now** | **My answer to the §3.2 question, measured rather than argued.** Full entailment is unavailable, but a real intermediate is: **every identifier-shaped token in a claim must appear in the evidence that claim cites** — device names, interface names, IPv4 addresses, prefixes, AS numbers, numerals. Deterministic, and the natural generalisation of `check_timeline_citations` (T-029b), which already does exactly this for timestamps and mnemonics. **Measured on round 3's real report: 0 false positives** once identifiers are canonicalised through `interface_kind`'s prefix table (`Gi0/0/0/0` ≡ `GigabitEthernet0/0/0/0`), and it catches A's counterexample — *"chassis power supply on PE7 failed, taking down 10.255.0.99"* flags `PE7` and `10.255.0.99` as unsupported. **It is not entailment**: it catches an invented *entity*, not a wrong *relation* between real ones, so it raises the floor while B-439 is built rather than replacing it | — | S | review §3.2 · my addition |
| **B-454** | **`temporally_incoherent` conflates a moved fabric with a slow collection** | **Found by round 5 (B-451), predicted by nobody.** Probe 09 was incoherent because the fabric moved -- `bgp_session` healthy at collection, broken at re-read -- which is the signal the finding exists for. Probes 08/10/11/99 were incoherent because collection took 34-38s against a 30s bound, with the re-read **agreeing every time**. Same finding string, same exit 2, indistinguishable to an operator. **And the width breach destroyed a correct answer**: probes 10/11/99 saw a settled, fully-converged broken fabric (`XXXX.`) whose lowest broken rung was `igp_adjacency` -- the right finding was `igp_isolated` and the bound replaced it with a refusal, four minutes after the fabric stopped changing. This build already has the right pattern and did not apply it here: `COVERAGE_LIMITED` keeps a real answer and labels it, on the reasoning that *a real answer at the wrong strength* beats no answer. A width-only breach is the same shape -- the finding stands, qualified. Instability is genuinely different. **Split them: skew-breach qualifies, disagreement refuses.** | B-436 | S | round 5 · my finding |
| **B-455** | **Skew is (sessions − 1) × an ~8 s device-side login penalty** | **Round 5 surfaced it as a correlation; measured afterwards it is a mechanism.** Back-to-back `connect + show clock` against RR1: **0.68 s, then 7.62, 8.52, 8.20, 8.72, 7.56**. After a 20 s pause: **0.54 s**, then 8.22, 7.77. So the **first** login after a gap is nearly free and every consecutive one costs ~8 s, and whether the penalty is armed at all varies with recent history — 12 back-to-back epochs earlier the same day showed none. This explains every skew reading in this build: 4 s (unarmed, 4 sessions) and 34–38 s (armed, 4 sessions ⇒ 3 × 8 s + work). **Consequences.** (1) The bound is not a property of the tool: an operator's willingness to get an answer depends on a device-side throttle nobody configured. (2) **Session count is the only lever the tool has**, and it is now 4 — 1 intent + 1 template batch per device. Getting to 2 means running intents and rendered templates in **one** session, which needs a runner that applies `is_approved` to one kind and `is_safe_rendered_command` to the other. That is additive and relaxes no validator, but it is the most safety-critical path in the repo — **specify it and bring it, do not write it**. (3) Meanwhile `to_payload()` discards the per-`Observation` spans that made this diagnosable — surface them, and note that discarding them is shape 7 inside code written to fix a different problem | B-436 | S | round 5 · my finding  **MEASURED AGAIN AT THE MCP SURFACE, 2026-08-17:** nine parallel `get_lab_device_facts` calls took **70 s, with connection retries on two devices**. **The MCP path re-introduces exactly the per-device session cost the epoch removed**, and it does so structurally rather than accidentally: the epoch works because a *flow* declares every device and command up front, so collection can be planned. On the MCP surface **the model decides the collection**, one tool call at a time, and a plan cannot be made for calls that have not been requested yet. So this is not a missing optimisation on that path — it is the same defect the epoch fixed, in a place where the epoch's fix does not apply, and `investigate_lab_session` is the only tool on the surface that escapes it. Two consequences: a model fanning out across a fabric will hit retries and may read a timeout as a device fault, and any per-tool latency measured through MCP is a measurement of the login penalty, not of the tool.  **FOURTH MEASUREMENT, from the MCP payload:** a healthy fabric, three sessions, no fault — **skew 24.4 s against a 30 s bound**, within bound, re-read agreeing. **Routine skew at 80% of the bound is a much thinner margin than the design assumed.** `evidence-epoch.md` §2.3a exists precisely so this would be visible rather than only appearing the first time it breached, and it has now paid for itself: the bound was derived as the fastest relevant protocol timer (IS-IS hold, 30 s) on the assumption that real epochs would land well inside it, and at 24.4 s a single armed login penalty puts a healthy fabric over. Two readings, both worth stating: the 30 s bound may be too tight for a login-penalised fabric, **or** three sessions is still too many and B-456 closing the last one is what restores the margin. The second is testable as part of B-456 and should be measured before the bound is touched — moving a threshold to accommodate a cost that is about to be removed would hide the improvement.  **CORRECTED 2026-08-17 (OBS-119): B-456 does NOT close the last session, and the floor is three, not two.** They were planned as one change. The fan-out manifest cannot be built until the reverse route is read, so a fan-out device needs two passes regardless -- what to collect second depends on what was read first. Measured with the penalty armed: 25-31s before B-456, 38-42s with the route in its own session, 29-33s once batched with the intents. Roughly neutral, not a saving.  **CLOSED 2026-08-17. The floor is THREE sessions and it is structural — recorded so nobody reopens this against a target that cannot be reached.** RR1 costs one; PE2 costs two. The second is not waste: **a fan-out device needs the reverse route before its manifest exists**, so what it collects second depends on what it read first, and no amount of batching removes a dependency between two reads. Measured with the penalty armed at each stage: 10 sessions before the epoch, 7 with per-template logins, 4 batched, 3 with the combined runner, and B-456 held it at 3 (29-33s) after briefly costing a 4th (38-42s) by giving the route its own pass. The only remaining lever is removing the fan-out itself, which is a different flow rather than an optimisation. |
| **B-456** | **Candidate: `no_fault_on_path` may be rare, and round 4 may never have been a case of it** | **The operator's candidate, filed to be considered and explicitly NOT acted on.** The argument: **rungs 3 and 4 cannot be off-path** — no route means no path, by definition, and an IGP-isolated subject device is not reachable by any path. So the only rung that can be broken while the path survives is the **interface** rung, and only because `EACH_PHYSICAL_INTERFACE` aggregates over *all* members while the path used a *subset*. **The route output already names its outgoing interface**, so the subset is knowable rather than inferred. If that is right, round 4 was never a B-428 case at all — it was the interface rung asking about every interface instead of the one the path uses, and a path-scoped interface rung would have returned `all_layers_healthy` with no new finding needed. **Consequence if confirmed:** `no_fault_on_path` becomes rare rather than routine, and B-428's predicate becomes a backstop rather than a load-bearing rule — which is a different and much smaller claim than the one currently in the code. **Consequence if wrong:** the aggregation is right and the off-path case is real, and B-437's discrimination criterion has to carry the whole weight. Note the interaction with B-454/B-457: this would reduce how often the off-path branch is reached at all, so it should be settled *before* that branch is hardened further. Links to reviewer A's interface-scope concern and to **B-431** — the member set is exactly what that item made declarable | B-437, B-431 | M | operator candidate · round 5 · A |
| **B-457** | **A systematic paraphrase-grounding regression is now invisible** | **Created by B-439, and the concern is inherited from the test it replaced.** The old rule was *a withheld report exits 2*, defended on the grounds that *"if a grounding failure exited 1, a systematic grounding regression would hide in the noise of routine faults forever."* That reasoning was right while the report **was** the answer. It is not now: the authoritative report is rendered, and exiting 2 because a restatement was clumsy would let a cosmetic failure suppress a trustworthy finding. **But the original concern is not thereby answered.** If every paraphrase silently stops grounding — a prompt change, a model change — nothing raises an alarm. The status is in the payload and on stderr, and **a field nobody aggregates is not detection**. Needs a real signal: a `metrics.py` counter for paraphrase grounding outcomes is the cheapest, since that module already exists and already counts health verdicts by severity. Note the shape — this is a *monitoring* gap created by correctly removing a *trust* gate, and the two are easy to conflate | B-439 | S | my finding · B-439 |
| **B-458** | **Residual: a transport exception can embed device output in an error string** | **The known gap left by the MCP boundary, bounded rather than claimed clean.** `sanitize` strips `data.commands` and `unaccounted_lines`, and keeps `errors` -- a model that cannot see failures is worse than one that sees a truncated failure. But netmiko's read-timeout exceptions can carry accumulated device output in their message, so an error string is a narrow raw-text vector. Currently truncated at `MAX_ERROR_CHARS = 400`, which bounds it and does not close it. Closing it means classifying error *kinds* at the transport boundary and emitting a structured error rather than a formatted string -- which is worth doing for its own sake, since `diff_evidence` and `_section_from_combined` already parse those strings by prefix | — | M | OBS-111 |
| **B-459** | **Argument fabrication — validate a tool's arguments against observed reality before executing** | **The operator's finding from the MCP experiment.** *(Filed as B-459, not B-458 as instructed: B-458 was taken an hour earlier by the error-string residual. Flagged rather than renumbered.)* **Every containment mechanism in this build operates on what a tool RETURNS. Nothing constrains what a model SUPPLIES.** The model needed a peer address the question did not contain and **asked** rather than inventing one — but that is one model on one occasion, not a property. A fabricated address would have produced a fully grounded, correctly cited investigation of a session **that does not exist**, and every gate in the build would have passed it: the descent would walk, the rungs would return `unevaluated` or `broken` honestly, the report would cite real evidence keys, and grounding would confirm the citations resolve. Nothing anywhere asks whether the *subject* is real. **The defence:** an argument naming a network object must appear in that device's own observed state before the tool executes — a peer address that appears in no BGP summary on that device is not a peer, and a prefix, an interface name and a ping target are the same class. **This is B-453 pointed the other way**, and that symmetry is the strongest argument for it: B-453 checks that every identifier in a model's *output* appears in the evidence; this checks that every identifier in a model's *input* appears in the device. Same mechanism, same declared canonicalisation through `interface_kind`, opposite direction. **Two honest limits.** It catches an invented object, not a wrong one — a real peer address that is not the one the operator meant passes, exactly as B-453 catches an invented entity and not a wrong relation. And validating costs a read *before* the read, which on this fabric is a session and an ~8s login (B-455), so where the check runs is a design question and not a detail | B-453 | M | MCP experiment · operator |
| **B-460** | **`state_pfx_rcd` holds two different fields under one CLI heading** | **Sharpened from "the name invites a misread" to what it actually is.** `show bgp summary`'s `St/PfxRcd` column is not a count that sometimes reads oddly — it holds **either a prefix count or a session-state string**, and which one depends on the session's state. The parser stores whichever appeared in a single field, so `state_pfx_rcd == "0"` and `state_pfx_rcd == "Idle"` are the same field carrying different *kinds* of value, and every consumer has to re-derive which kind it got. `checks.py` does exactly that in three places. **This is a stronger argument than ergonomics: a field whose type depends on its value cannot be checked, diffed or compared without first reconstructing the discriminator the CLI threw away.** It already bites — `diff_evidence` reports a transition from `"0"` to `"Idle"` as a value change in one field, when it is a session going down. **Split into `session_state` and `prefixes_received`**, both always present, with `prefixes_received` absent (not zero) when the session is not Established — the same `router_id`/`local_as` rule Phase 3 already applies for devices with no BGP process. Note the audit interaction: `test_field_audit.py` pins the parsed/read split as numbers, so this moves them and the commit must say which field and why | — | S | field-split · operator  **The defect is LIVE, not prospective, and that raises it above a naming improvement.** `diff_evidence` compares parsed records field-by-field, so a session going Established → Idle is recorded as `state_pfx_rcd` changing from a prefix count to a state string — one field, one value change. **`detect_flaps` reads those same diffs**, so a session bouncing Established/Idle/Established is counted as *a string oscillating*, not as *a session flapping*. The flap detector was built specifically because a peer that bounces looks clean in every pairwise diff, and for BGP session state it is currently detecting the right transitions **for the wrong reason and under the wrong name** — which means its output cannot be filtered to "sessions that flapped" without re-deriving the discriminator the CLI threw away. Split into `session_state` and `prefixes_received` and both problems close at once.  **CLOSED 2026-08-17.** Split at the parser, where the discriminator is still available: `session_state` always, `prefixes_received` **absent (not zero)** when the session is not Established — the same rule Phase 3 applies to `router_id`/`local_as`, and for the same reason, that zero is a measurement and absence is not. The three `_is_numeric` re-derivations in `checks.py` are gone. The flap detector now reports `session_state` oscillating Established/Idle rather than a string changing type, and `bgp_no_prefixes` reads `prefixes_received == 0`, so a down session no longer risks reporting a prefix problem on top of its real one. |
| **B-461** | **The unowned surface: a chat client restating our output is a boundary we do not own** | **A distinct boundary, not an instance of B-439** — recorded separately on the operator's correction, because collapsing them hides which half is fixable. B-439 governs **our** `paraphrase` field: produced here, graded here, marked non-authoritative here. **This is different in kind.** A chat client restating a *correct* deterministic report degrades it downstream of every gate in the build, on a surface with no field to mark, no grounding hook and no instrumentation. Observed live (OBS-115): five rungs became four, and two PE2 rungs were attributed to RR1. **Nothing here can be enforced, and the item must never be written as though it can.** What is available is *detectability*: the report shape now states `rungs_examined` and numbers each observation `1/5 … 5/5` with its device, so a four-item restatement is visibly short to a human reading both. That is the sub-item under B-439 and it is done. What remains open is everything else this boundary implies — the same degradation applies to the causal chain, the coverage caveat and the `off_path` list, and none of those is numbered or counted. **Open question for the operator:** whether an unowned surface is worth designing further for at all, or whether the honest position is that the payload is the artefact and prose about it is not the product | B-439 | M | OBS-115 · operator |
| **B-462** | **Round 7 — does a down interface persist in the route table as an LFA backup?** | **The gating question B-456 could not settle (OBS-118).** Path scoping takes the interface member set from the route's named paths, which is only sound if a down port leaves the route table promptly. The committed corpus **cannot answer it**: 70 routes with a path, zero naming a down interface, and zero devices with a down port *and* a surviving route — so the zero is vacuous, not reassuring. **The round:** shut one uplink on a device with two (round 4's topology, so it is cheap to arrange) and sample the route table on that device through reconvergence, asking whether the down interface appears as `Backup (Local-LFA)` and for how long. Round 5's sampler runs it unchanged. If it persists, `EACH_PATH_INTERFACE` needs a liveness cross-check against `show interfaces` before a named path counts as a member — which is a different rung, not a tweak | B-456 | S | OBS-118 |
| **B-463** | **Round 8 — a fault that leaves TCP up and BGP down** | **The one gap B-437's audit found.** The `bgp_session|transport` boundary is separated only by a composed vector; every captured fault so far breaks both rungs together. Since B-432, rung 2 reads the **TCP socket**, so separating it needs a fault where **the socket connects and the FSM never reaches Established** — an AS mismatch, an MD5/authentication mismatch, or a hold-timer mismatch. Any of the three is a one-line config change on a single neighbour and fully reversible. Expected vector `BHHHH`, finding `cause_not_localised`, which would also replace this build's last composed fixture with a captured one. Note the shape: **rung 2 was given a distinct subsystem fifteen commits before anyone checked whether the fabric could exhibit the distinction** — a capability added and never exercised is indistinguishable from one that does not work | B-437 | S | B-437 audit |
| **B-464** | **Round 9 — capture `transport_blocked` from an MD5 mismatch** | **Split off from B-463 (round 8), which changed fault.** MD5 **provably cannot** close the rung 1|2 boundary: TCP MD5 breaks TCP, so the socket never arms and rung 2 is broken by the same failure as rung 1 (`ROUND-8.md` §1.1). Running it would spend a lab window confirming analysis that is already sound. **What it is still worth:** a captured `transport_blocked` from a *second, independent mechanism* beside round 3's administrative shutdown, and a `last_reset_reason` no capture currently holds. Lower priority than any round that closes a boundary. `fault_lab.py` option 6, unchanged. The sealed MD5 prediction stands as a recorded unrun claim in `ROUND-8.md` §3 | — | S | ROUND-8 §1.1 |
| **B-465** | **Every IS-IS baseline is stale, and drift now fires on the repair** | The `expected:` blocks were derived by `learn-topology` from the broken fabric and never re-derived. PE2 records `isis_adjacencies: 0` and the MCP re-test observed **2**; PE1 records `1` and observed `2`. **`suspicious_baseline` catches only the two recorded as `0`** — PE1's `1` is equally wrong and structurally invisible to it. **The new half of the problem:** the known failure of a baseline learned from a broken fabric is that it *suppresses* the fault it learned from; this run shows the second half — **once the fabric is repaired the same baseline emits false drift alarms**, and `isis_adjacency_count_drift` reporting a repair is indistinguishable from reporting a regression. Two pieces of work: re-run `learn-topology` against the current fabric, and give `suspicious_baseline` a way to flag *any* baseline that disagrees with a role invariant, not only a zero | — | S | OBS-142 · MCP §10.4 |
| **B-466** | **The coherence bound runs at 77% on a healthy fabric** | `evidence-epoch.md` §3 asked *"if real epochs land at 25 s against a 30 s bound, that is a finding"* and could not answer it. The MCP re-test measured **`skew_seconds: 23.117` of `bound_seconds: 30.0`** on a routine, uneventful, all-healthy descent. Not a violation and not comfortable. The bound was derived as the fastest relevant timer (IS-IS hold), never fitted to observed behaviour. **The consequence is a refusal mode nobody has costed:** one slow device, one connection retry, or the ~8 s consecutive-login penalty (chaos-harness §6.1c) landing twice inside one epoch pushes a healthy fabric to `temporally_incoherent` and exit 2 — **a refusal caused by the tool's own latency on a fabric with nothing wrong with it**. Needs the skew distribution across several runs before the bound is either widened or defended | B-436 | M | OBS-142 · MCP §10.6 |
| **B-467** | **Free-text device fields reach every model call, typed as safe by being parsed** | The invariant guards *unparsed* text and `sanitize()` honours it — but parsing a log line makes its *shape* known, not its content safe. Measured: `get_lab_logging` ships 17,916 chars of device prose under `parsed.records[].text` after sanitisation, and `build_correlate_prompt` embeds all 28 shaped records verbatim on the trusted path. Syslog is attacker-writable from the network (a failed login embeds the chosen username). What contains it today is output-side: grounded timelines, B-453 containment, non-authoritative labelling — an injected line cannot fabricate a *cited* finding, but it can steer prose. Fix is typing, not stripping: provenance-tag free-text fields at parse time, render them in delimited quote blocks, cap per prompt. **Do this as part of the model-egress projector or the projector rebuilds the hole one layer up** | — | M | DEEP-REVIEW §2.1 |
| **B-468** | **`nettools ping` exits 0 on 100%% packet loss; `unsupported` exits 1** | Both measured. `_cmd_ping` maps transport success to exit 0 while `loss_pct: 100` sits parsed and unread in the payload — four decades of `ping && next-step` convention inverted at the exact place muscle memory is strongest. And `_cmd_check` has two buckets, so `unsupported` (documented three times as *not a failure*) exits 1 from the per-device CLI while `check_fabric` stays green on the same fact. Cheap, operator-facing, do before the full result-envelope work | — | S | DEEP-REVIEW §2.2 |
| **B-469** | **A blanket catch silently flips rung 5's semantics on the trust-loss path** | `except (ValueError, Exception)` around `origin_prefix_for` converts *any* failure — an inventory gap or a future typo — into `origin = None`, which switches rung 5 from the path-scoped `ANY_HEALTHY` member set to the all-interfaces `ALL_HEALTHY` fallback. That fallback is the exact residual `ROUND-6.md` §2.3 seals as the remaining reviewer-B exposure, so a programming error here reintroduces the trust-loss scenario fabric-wide with every collector-injecting test still green. Narrow to the errors meant; surface `origin_unresolved: <reason>` in the payload | — | S | DEEP-REVIEW §2.5 |

---

# B-426 in full — fault injection

Larger than a backlog row, and framed deliberately as **the Stage 2 acceptance vehicle rather than a test tool**.

## Why it is not just a test tool

Stage 2 is `inject → device syslog → Alertmanager → agent wakes → investigates → reports`. Every component of that can be verified alone and the loop still not work: a webhook receiver tested against a hand-crafted POST tells you the receiver parses JSON, not that a real fault on a real device produces an alert carrying a device label that resolves to a flow that produces an investigation. **Injection is the only way to exercise the loop as one thing.** Noted against B-201.

## Mandatory requirements

| | Requirement | Why |
|---|---|---|
| 1 | **A supervisor process independent of the injector**, periodically diffing the whole fabric against the golden snapshot and force-restoring | OBS-075: the injector's own `restore()` reported failure on a healthy fabric and would have retried a non-idempotent write. A restorer that shares a process with the injector shares its bugs. `save_golden_snapshot`/`diff_evidence` already exist and are the right primitives |
| 2 | **Max one concurrent fault, a hard hold ceiling, and a kill switch** | Two faults is a *deliberate* experiment (below), never an accident of overlapping drills. The ceiling bounds the blast radius of a supervisor that has itself died |
| 3 | **The agent must not know a drill is running** | A blind trial is the only kind whose agreement means anything. See the operating rule below |
| 4 | **A holdout fault set, never used for tuning — only for validating fixes** | Otherwise every fix is fitted to the faults that found the bugs, and the corpus stops measuring generalisation. The same reasoning as `evidence-reduction.md`'s "tuning until output looks reasonable" |
| 5 | **Two-fault combinations** | Currently untested, and the place D6's "lowest broken rung is the root cause" may simply be wrong — see D6's open section and OBS-078 |

## Operating rule — the agent never runs the injector

**Two independent reasons, and the second is the one that is easy to forget.**

1. **It writes to devices.** §0.11, unwaived. Nothing about a drill changes that.
2. **Running it would put the fault identity in the agent's context, which destroys the blinding the trial depends on.** This is a *different* kind of prohibition from §0.11's: not "this action is dangerous" but **"performing this action makes me a worse witness."** The harm is not to the fabric, it is to the evidence — and it is silent, because a contaminated trial produces exactly the same confident agreement a clean one does.

The injector is operated by a human or by a separate process. Recorded in `BUILD-PLAN.md` §0.11 so it is binding rather than advisory.

## Sequencing — four manual rounds before any of this is built

**One match is one data point.** T-033 produced agreement on `igp_adjacency`; that is encouraging and it is not a pattern. Build the harness only if agreement holds **across four rounds landing on different rungs** — four rounds of the same fault shape is one data point sampled four times, which is §0.13's data face.

**Why different rungs, and not simply more rounds.** Rounds 1 and 2 exercised the walk rule in **opposite directions**, and neither would have caught a defect in the other's direction:

| | What the walk had to do |
|---|---|
| **Round 1** (`igp_adjacency`) | **descend past a healthy rung** to a broken one below it, and report the lower one |
| **Round 2** (`transport`) | **not descend into three healthy rungs**, and report a cause above them |

Same rule, opposite pressure. A walk that stopped at the first broken rung passes round 2 and fails round 1; a walk that always reported the deepest *evaluated* rung passes round 1 and fails round 2. **Four rounds on one rung could not have distinguished either defect from correct behaviour** — they would be one data point sampled four times, which is §0.13's data face applied to a trial sequence.

That is the argument for the coverage below: each round is chosen for the *pressure it puts on the rule*, not for variety.

Suggested coverage, chosen so the rounds differ in kind and not just in device:

| Round | Target | Why this one |
|---|---|---|
| 1 ✓ | `igp_adjacency` on PE3 | **Match** (OBS-077). Symptom identical to a different-rung fault. Required *descending past* a healthy rung |
| 2 ✓ | `transport` on RR1 | **Match** (OBS-088). Required *not descending into* three healthy rungs — the opposite pressure. Scored a vacuous pass on resolution (OBS-089) |
| 3 ✓ | `cause_not_localised` attempted — landed `transport_blocked` | BGP session administratively shut with a healthy underlay. **The only composed fixture in the build** — this round replaces it with a captured one. **Refutation branch fired** (OBS-092), specified in advance and scored as a corpus result. Did **not** replace the composed fixture — B-432 explains why it may not be capturable at all. Surfaced B-430, B-431, B-432 |
| 4 ✓ | `all_layers_healthy` under perturbation — **predicted failure occurred** | One uplink shut so the IGP reconverges. A **true negative** is the outcome a corpus of faults never tests, and the one a false-positive-prone system fails. **Outcome predicted in advance (OBS-082)** |

Each round produces a row for B-427 before the harness exists. The harness automates producing rows, not deciding what they mean.

---

# What is deliberately absent

Recorded so their absence reads as a decision.

| Not here | Why |
|---|---|
| Vector search over vendor documentation | Chunking destroys the submode hierarchy that *is* the information, and the retriever becomes a contamination vector |
| A generic `run_command` tool, in any form | Moves the safety decision to runtime. The whole architecture exists to avoid this |
| Model-written memory | Reads its own hallucinations back as evidence and compounds them |
| Autonomous remediation of novel failures | There is no Stage 4 |
| Fine-tuning | Not a fix for an output-space control problem. Revisit only if grammar-constrained decoding also fails |
| A workflow engine on top of the read path | Diagnosis is adaptive; a deterministic graph is an automation script |
| Multi-tenancy / per-user scoping | Single-operator deployment. Would need B-301 first anyway |

---

# What happens next — three tracks

**Superseded "The next three" on 2026-08-16.** MVP-0 is complete (M4) and the review is written. Work resumes on three parallel tracks, on separate branches, **one agent each**.

| Track | Branch | Model | Scope | |
|---|---|---|---|---|
| **A** | `feat/validation` | **Opus 5** | The four injection rounds, then **B-428** | **CRITICAL PATH** |
| **B** | `feat/log-evidence` | Sonnet 5 | **B-414**, **B-416**, **B-418** — captured corpus only | |
| **C** | `feat/chaos-harness` | Sonnet 5 | **B-426**, **B-427** — **outside the repository entirely** | |

**Merge order: A, then B, then C.**

## Rules

1. **Opus 5 judges acceptance on every track. Sonnet 5 never decides.** §0.9 unchanged and now operating across three branches at once rather than one.
2. **Any change to a shared contract is a HALT on that track, not a decision.** The contracts are enumerated below, because a HALT rule nobody can apply is not a rule.
3. **One agent per track, never two.**
4. **B-416 feeds B-428 but must not be coupled to it.** Track B builds episodes for **correlation quality on their own merit**; track A decides *later* whether to consume them. Two agents editing one contract is the failure mode rule 2 exists for, and this is the pair most likely to produce it — episodes are the timeline signal for masked second faults (OBS-078), which makes coupling them feel natural and would put both agents in the same file.

## The shared contracts

Touching any of these is a HALT on the track that touched it.

| Contract | Owner | Why it is shared |
|---|---|---|
| `descent.DescentResult` | **A** (B-428) | `prompt_library`, `grounding`, `investigation` and `output` all read it |
| `flows.Rung` / `CollectStep` | **A** | The ladder shape; `descent` and `investigation` both walk it |
| `checks.CheckResult` | **A** | Every rung produces one; `grounding` reads its evidence keys |
| `log_window.ShapedWindow` | **B** | `prompt_library.build_correlate_prompt` and `grounding.check_timeline_citations` both consume it. **B-414/B-416 will want to add aggregated records and episodes to it** — that is the collision |
| `coverage.Coverage` | shared | Built by B's code, consumed by A's gate |
| `grounding.GroundingResult` | **A** | Both gates return it; adding a counter is additive, changing `ok`/`vacuous` semantics is not |
| `prompts/*.txt` + `tests/cases/*.json` | **A** | Versioned; a change is a version bump under the library's own rules |
| `investigation.py` | **A** | **The most likely merge conflict.** It wires every module both tracks touch |

Track C touches none of them by construction — it lives outside the repository — which is why it merges last and carries the least risk.

## After the tracks

**The five flows (B-107–B-111) do not start until B-428 lands.** Then `isis_adjacency` **serially and alone**, to prove the pattern repeats on a second flow. Only then do the remaining four run in parallel.

The reason is in MVP0-REVIEW §4: the descent has produced **one** correct blind diagnosis, on one flow. Spawning five concurrent flow implementations off a pattern demonstrated once would replicate an unvalidated assumption five times, which is §0.13's data face applied to project planning rather than to a corpus.

## Deliberately not first

- **B-113** (MCP consolidation) — highest regression risk in the backlog.
- **B-401** (Juniper) — **parked**, no device to capture from. The payoff test remains unvalidated, not passed.
- **B-101/B-102** (the reasoning gate) — the first place a model influences control flow, and it assumes the descent is trustworthy. That is what track A establishes.
